apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: k8s-monitor
  labels:
    app: ollama
    component: ai-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  # storageClassName 은 오버레이에서 patch 로 지정 (클러스터마다 다름).
