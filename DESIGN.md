# DESIGN.md — UX/UI 운영 문서

> **역할**: PEP 의 UX/UI 를 **현행화 → 개선포인트 → 고도화** 사이클로 운영하는 단일 관리 문서.
> **운영 주체**: `.claude/agents/ux-ui-designer.md` (전담 UX/UI 디자이너 에이전트) — 절차는
> `.claude/skills/ux-ui-designer/SKILL.md` 를 따른다.
> **규격 원천과의 관계**: 토큰/팔레트/컴포넌트 **규격**은 `DESIGN_SYSTEM.md`, 화면 **명세**는
> `docs/SCREENS.md`. 이 문서는 그 규격이 실제 코드에 얼마나 적용돼 있는지 **점검·기록·운영**한다.

사용법 예시: "DESIGN.md 현행화해줘" / "디자인 감사 돌려줘" / "D-003 처리해줘".

---

## 1. 현행화 (Current State)

_최근 감사일: **2026-09-10** (2회차 정량 재집계 — `frontend/src` grep. pages 70 / components tsx 275. 1회차: 2026-07-19)_

> **2회차 정량 재집계 (2026-09-10, Main UI 상용화 진단과 함께)**: raw hex **104**(↓26) · 인라인 style **230**(↑10, 그래프/캔버스 좌표 계산 위주) · 고정 팔레트(`white/black/gray-*`) **37** · **`zinc-*` 57건(신규 지표)** — 기존 grep 패턴이 `gray-*` 만 잡아 `zinc-*` 고정 팔레트(네비 flyout·툴팁 등 셸 핵심부)가 1회차 감사에서 통째로 누락돼 있었다. 절차 A 의 스캔 명령에 `zinc-` 를 추가한다(D-071).

### 1.1 디자인 시스템 요약

- **테마**: 다크 모드 "Ops Slate" (slate-900 배경 + status 4색) 기본, 라이트 macOS 팔레트 보조.
- **레이아웃**: MacCard 섹션 카드 + ClusterSidebar(iconOnly, 56px 레일) + 12-col Bento Health Hero.
- **타이포**: Inter(+Pretendard fallback) 본문 / JetBrains Mono 숫자·코드.
- **차트**: Bullet Chart(SVG) · Sparkline · CheckHistoryHeatmap — Gauge/Pie 비추천.
- **DESIGN_SYSTEM.md 적용 로드맵 상태**: W3(Health Hero)·W4(접근성 패스)·W5(차트 교체) 완료,
  W1(raw hex 전수 치환)·W2(shadcn 5종 도입) 진행 중.

### 1.2 정량 준수 지표 (스냅샷)

| 지표 | 건수 (2026-07-19) | 목표 | 비고 |
|---|---|---|---|
| JSX 내 raw hex 색상 | 130건 / 22파일 → Recharts/SVG 계열 이관 완료 | 화이트리스트 외 0 | D-005 처리: `--chart-1~8` 신설 + 4파일 이관. 잔존은 캔버스/three.js 허용 예외 |
| 인라인 `style={{...}}` | 220건 / 71파일 | 동적 계산 외 0 | 상위 파일은 그래프/캔버스 좌표 계산으로 정당. 색·배경 하드코딩성 인라인 혼재 (D-008) |
| 고정 팔레트(`text/bg-white·black·gray-*`) | ~~338건~~ **오탐 정정** → 실측 110건 (스크림 62 · 유색 배경 위 text-white 등 43 · gray-* 5) | gray-* 0 | D-003 처리: gray-* 5건 토큰 치환 완료. 스크림·유색 배경 위 text-white 는 허용 예외 |
| 카드/버튼의 컨벤션 외 라운딩 | 해소 | 토큰 기준 준수 | D-002 판정: `rounded-md`=radius 토큰이 정본, 문서를 현행화. D-006: button `sm` 라운딩 base 통일 |
| ClusterSidebar 비-iconOnly | **0건** (26개 사용처 전부 준수) | 0 | ✅ 완전 준수 |
| 페이지 내 `<select>` 클러스터 선택기 | **0건** | 0 | ✅ 완전 준수 (pages 의 select 40여 개는 전부 필터/폼/페이지네이션용) |
| MacCard 미사용 수제 카드 div | 116줄 / 약 20개 페이지 → 1차 트랜치 8개 카드 전환 | 수렴 | D-004 1차: Settings·InfraTopology·NodeImages 완료. 잔여는 2회차 감사에서 재산정 |
| 접근성 (`aria-*` / 아이콘 버튼 라벨) | 공용 컴포넌트 폴더 아이콘 버튼 100% 라벨 보유 | aria-label 병행 | D-007 처리: 공용 4개 폴더 전수 보강(33건). pages/ 하위는 2회차 감사에서 점검 |

### 1.3 허용 예외 (위반으로 세지 않음)

| 예외 | 사유 |
|---|---|
| three.js/canvas/Recharts 계열 파일의 hex (`FlowGraph3D`, `Topology*`, `*Chart*`, `*Timeline`, `KanbanSummaryCharts`) | CSS class 를 받을 수 없는 렌더러 — DESIGN_SYSTEM.md W1 화이트리스트 |
| 컬러픽커 기본값 prop (`defaultBg="#..."`) | 색상 값 자체가 데이터 |
| 외부 서비스 고유색 (Jira `#0052CC` 등) | `brand.*` 토큰 경유로 관리 |
| portal/tooltip 위치 계산 인라인 스타일 | 런타임 좌표 — Tailwind 로 표현 불가 |
| `bg-black/60` 모달 스크림 (~62건/20파일) | 레포 공통 모달 백드롭 컨벤션 — 테마 중립(양 테마 성립) |
| 유색 배경(status/primary) 위 `text-white` | 대비가 배경색으로 보장됨 |
| WebGL(ForceGraph3D) 엣지 alpha-hex (`#ffffff55` 등) | WebGL 링크 블렌딩이 alpha-hex 직접 파싱 — 토큰 이관 시 동작 보장 불가 (코드 주석 명시) |

---

## 2. 개선포인트 백로그 (Backlog)

상태: `대기` → `진행중` → `완료`/`보류`. ID 는 재사용 금지(행 삭제 금지, 상태만 갱신).
심각도: `높음`(사용성·접근성 실질 저해) / `중간`(일관성 훼손) / `낮음`(cosmetic).

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-001 | 문서 정합성 | CLAUDE.md UI 섹션은 "macOS 라이트 기본"으로, DESIGN_SYSTEM.md 는 "다크 기본(Ops Slate)"으로 서술 — 기본 테마 서술 충돌 | 신규 화면 작업 시 기준 혼선 → 화면 간 톤 불일치 유발 | 중간 | 완료 | 실측: 기본값은 `'default'`(Claude 브랜드 톤), light/dark 는 Databricks 계열 대안. 양쪽 문서 서술 통일 (`6bac1cf`) |
| D-002 | 컴포넌트-문서 불일치 | `MacCard` 기본 'flat' variant 가 `rounded-md` 렌더 (`MacCard.tsx:66`) — 문서 컨벤션(카드 `rounded-2xl`, 기반 `card.tsx` 는 2xl)과 충돌 | 페이지마다 카드 라운딩이 달라져 시각 일관성 훼손. 신규 작업 시 기준 혼선 | 중간 | 완료 | 판정: `rounded-md` 는 `var(--radius)` 기반 **테마 인지 토큰**이라 코드가 규격상 옳음 → 문서 현행화로 해소. CLAUDE.md UI 섹션 전면 갱신 + frontend-page 스킬 문구 갱신 (`6bac1cf`) |
| D-003 | 다크모드 | 고정 팔레트(`text/bg-white·black·gray-*`) 338건/106파일 — `LakeServiceTypeManager`(18) `ServiceCategoryManager`(13) `WorkItemTableRow`(12) `WorkItemBoardPage`(10) 등 설정·워크아이템·서비스 도메인에 집중 | 다크모드에서 대비 저하·가독성 손상 가능 | 높음 | 완료 | **1차 감사 338건은 과대집계 오탐** — 상위 파일들은 기존 작업에서 이미 토큰화 완료 상태였음. 실측 잔존 gray-* 5건(3파일)만 `status-unknown` 토큰으로 치환 (`4c8ab30`). `bg-black/60` 모달 스크림 62건은 허용 예외 등재 |
| D-004 | 일관성 | 약 20개 페이지가 MacCard 없이 수제 카드 div(`bg-card border rounded-*`) 사용 — `SettingsPage`(9) `InfraTopologyPage`(7) `NodeImagesPage`(6) 등 | 카드 헤더·라운딩·구분선이 페이지마다 달라짐 | 중간 | 완료 | 1차 트랜치: Settings(4)·InfraTopology(1)·NodeImages(3) 섹션 카드 8개 전환 (`2868cb5`). stat 타일/리스트 행/모달 내부는 컨벤션대로 유지. 잔여 페이지는 2회차 감사에서 재산정 |
| D-005 | 시각화 토큰 | 차트·다이어그램 raw hex 130건/22파일 (`MindMapPage` 33 · `OntologyPage` 15 · `KanbanSummaryCharts` 9 등) — 허용 예외이나 색이 테마와 무관하게 고정 | 다크/라이트 전환 시 차트 대비 불균형 | 중간 | 완료 | `--chart-1~8` 토큰 신설(3테마) + Kanban/ClusterTrends/WeeklyTimeline/VersionGraph 이관, three.js 는 `chartTokenColor()` computed-style 헬퍼 (`7886815`). MindMap/Ontology/FlowGraph3D 캔버스는 허용 예외 유지 |
| D-006 | 컴포넌트 규격 | `button.tsx:22` `sm` variant 가 `rounded-lg` (base 는 `rounded-xl`) — 소형 버튼이 시스템 차원에서 라운딩 이탈 | 소형 버튼 톤 불일치 (경미) | 낮음 | 완료 | `sm` 의 `rounded-lg` 오버라이드 제거 → base `rounded-xl` 상속으로 통일 (`6bac1cf`) |
| D-007 | 접근성 | 아이콘 전용 버튼이 `title` 위주(718/1084) — `aria-label` 병행이 표준화돼 있지 않음 | `title` 은 스크린리더 지원 신뢰도가 낮아 보조기기 사용성 저하 | 중간 | 완료 | 규칙을 CLAUDE.md 컨벤션에 명문화 + 공용 컴포넌트 4개 폴더(common/layout/ui/dashboard) 전수 적용 — 18파일 33건 추가(기존 title 복제 21 + 신규 12). WorkCalendar 날짜 셀 `role="button"` 은 구조 리스크로 보류 기록 |
| D-009 | 일관성 | D-004 잔여 — MacCard 미사용 페이지 2차 트랜치: NodeLabels·KernelParams·McClient·TrendDigest·TodoToday·Versions·EtcdCtl·KnowledgeHub·JiraExcelImport (9개) | 카드 스타일 페이지별 상이 | 중간 | 완료 | 9개 페이지 섹션 카드 28건 전환 (`6b65c05`·`3d79624`·`a8256bd`). 보드/캔버스형(MindMap·Workflow·Ontology·PacketFlow·WbsFlow)과 HomePage 는 구조 리스크로 보류 — 별도 정성 리뷰(R-4)에서 판단 |
| D-010 | 접근성 | D-007 잔여 — pages/ 하위 아이콘 전용 버튼 aria-label 미병행 | 보조기기 사용성 저하 | 중간 | 완료 | pages/ 전수 스윕 완료 — 25파일 83건 추가 (A~J 17 · K~Z 59 · 잔여 9페이지 7). `IconBtn`(K8sManage) 공용 컴포넌트에 aria-label 기본 배선. 클릭 가능한 `<tr>`/`<th>` 행 네비게이션은 키보드 접근 불가로 보고만 — R-3 후보 |
| D-008 | 인라인 스타일 | 시각화 외 파일의 색·배경 인라인 하드코딩 혼재 (예: `MacCard.tsx:48` `style={{ background:'var(--mac-red)' }}` 류) | 토큰 우회 경로가 남아 테마 관리 어려움 | 낮음 | 완료 | MacCard 신호등 인라인 → Tailwind arbitrary 클래스, ViewModeBar 의 깨진 `color: var(--muted-foreground)` (HSL triplet 원시 사용 버그) 수정 (`6bac1cf`). 동적 좌표 계산은 허용 예외 유지 |

### R-4 1차 라운드 발견 (홈·대시보드·운영점검·업무 — 2026-07-20)

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-011 | 상태색 토큰 | **상태색 고정 팔레트(`-400/-500`)가 4화면 전부에 재발** — Dashboard `statusColor`(59-64)·AddonCard 전반·툴바 버튼, HomePage KpiPill accent·DayScheduleBoard `STATUS_STYLE`(57-63), OpsCheckConsole `text-emerald-500`(305), WorkItemTableRow `KS_DOT/KS_TEXT/PRI_STYLES`·담당자칩·헤더배지 | `-400` 톤은 다크 기준이라 라이트/default 테마에서 대비 급락 + 화면마다 "같은 정상색"이 달라 스캔 학습 붕괴 | 높음 | 완료 | 4화면 전부 `--status-*` 토큰 전환 — Dashboard/AddonCard(`1d32d7e`), HomePage/DayScheduleBoard/WeeklyTimeline(`1d32d7e`), OpsCheck(`b31714d`), WorkItemBoard/Row(`f42ae63`). 무의미 구분색은 중립/`--chart-*`, Jira 브랜드색은 유지 |
| D-012 | 에러 상태 | API 실패를 빈 상태로 위장 — HomePage/DayScheduleBoard/WeeklyStatusTimeline `isError` 미처리, OpsCheckConsole 카탈로그 `isError` 무시하고 "항목 없음"(42,213) | 백엔드 장애·인증만료를 "정상이라 비어있음"으로 오인 → 실이슈 누락 | 높음 | 완료 | HomePage KpiPill·DayScheduleBoard·WeeklyTimeline·OpsCheck 카탈로그에 isError 분기(배너+재시도) 추가. 빈 상태는 성공+빈 배열일 때만 (`1d32d7e`·`b31714d`) |
| D-013 | 접근성 | 인라인 편집/클릭 요소 키보드 접근 불가 — WorkItemTableRow `EditableCell`(101-109) `td onClick`·담당자칩 span, (R-3 후보였던 클릭 `<tr>`/`<th>` 포함) | 마우스 없이 편집·행 이동 불가 — 핵심 워크플로가 키보드/스크린리더에 닫힘 | 높음 | 완료 | WorkItemTableRow EditableCell·담당자칩에 `role=button+tabIndex+onKeyDown(Enter/Space)+aria-label+focus:ring` (`f42ae63`). 클릭 `<tr>`/`<th>` 행 네비게이션은 R-3 로 이관 |
| D-014 | 인터랙션/안전 | OpsCheckConsole SSH(`batch_job`)·Ansible(`playbook`) 실행이 확인 없이 즉시 트리거(114-123,280) — 조회성 deep_check 와 동일 무확인 버튼 | 오클릭 시 실서버에 원격 명령. 전체선택 일괄실행 시 위험 규모 큼 | 높음 | 완료 | 사용자 결정: **모든 실행을 운영 위험 레벨로 간주**. 개별·선택 실행 전 ConfirmDialog(danger) — 클러스터명·건수 + 소스별(SSH/Ansible/점검/애드온) 대상 목록 요약. 즉시 실행 경로 제거 |
| D-015 | 접근성 | 색 단독 상태 전달 — OpsCheckConsole `lastStatus` StatusDot 만(267, 옆 텍스트는 시각뿐), CheckHistoryHeatmap 색만+화면 범례 없음(107) | 색맹·스크린리더 사용자가 과거 결과 정상/위험 판별 불가 | 중간 | 완료 | OpsCheck lastStatus 를 StatusBadge 로 교체(`b31714d`) + CheckHistoryHeatmap 상시 범례(색+라벨) 추가(색맹 안전). **완료** |
| D-016 | 일관성/레이아웃 | MacCard·ClusterSidebar 표준 이탈 — WorkItemBoardPage 3 wrapper 직접 `bg-card border rounded-xl`(610,622,643), Dashboard 행 `mx-auto px-3`(351)로 보조 사이드바 flush 규칙 위반 | 카드·레일 위치가 다른 화면과 어긋나 전환 시 점프 | 중간 | 완료 | WorkItemBoard wrapper 3곳 MacCard 전환(`f42ae63`), Dashboard 행 `mx-auto px-3`→`pr-3`(`1d32d7e`) |
| D-017 | 로딩 상태 | 로딩 비일관 — HomePage `useWorkItems` `isLoading` 미반영해 3필 "0"으로 튐(73), PlatformStatusMatrix 텍스트 로딩(170), WorkItemBoard skeleton 컬럼 미반영(621), HealthHero 스켈레톤 `rounded-2xl` 시프트 | 로딩→로드 전환 시 오정보·레이아웃 시프트 | 중간 | 완료 | HomePage `useWorkItems` isLoading 반영해 KPI 필 "…" 통일(`1d32d7e`). PlatformStatusMatrix 텍스트 로딩→매트릭스 구조 skeleton, WorkItemBoard 칸반 skeleton 을 실제 5컬럼(헤더+카드) 구조로, HealthHero 스켈레톤 `rounded-2xl`→`rounded-md` 토큰. **완료** |
| D-018 | 접근성/일관성 | OpsCheckConsole 상세 모달이 raw `fixed inset-0`(341) — Escape·포커스트랩·`role=dialog` 없음, `rounded-2xl` 레거시 | 키보드로 모달 열고닫기 불가, 배경 포커스 누수 | 중간 | 보류 | shadcn `Dialog` 로 교체(R-2 연계) — 리팩터 규모로 다음 라운드 |
| D-019 | 반응형/정확성 | 기타: HomePage work 모드 xl 미만 스크롤 중첩(171), OpsCheckConsole 테이블 `overflow-x` 컨테이너 부재(218), CheckHistoryHeatmap UTC `dayKey`(30-42)로 KST 날짜 경계 오프셋, 삭제 다이얼로그 문구/빈 따옴표(733), WIP 배지 ⚠ 이모지 의존(390) | 랩톱 폭 밀도 저하, 심야 점검 이력 오배치, 삭제 대상 오인 | 중간 | 완료 | 히트맵 KST dayKey(`62be648`)·삭제 다이얼로그 문구/빈 따옴표(`f42ae63`) 처리. HomePage work 모드 그리드 높이 채움을 xl 이상만 적용(xl 미만 패널 min-h+단일 스크롤)로 이중 스크롤 해소, OpsCheck 테이블 `overflow-x-auto`+min-w 래핑, WIP ⚠ 이모지→`AlertTriangle`(aria-label) 2곳(+Kanban WIP 배지 고정팔레트→status 토큰). **완료** |

### R-4 2차 라운드 발견 (K8s상세관리·K8s자원관리·일일점검리뷰·LAKE — 2026-07-20)

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-020 | 상태색 토큰 | **상태색 고정 팔레트가 4화면 전부 재발** — K8sManage `CELL_BG/STATUS_TEXT`(30-35)·노드점·배지, K8sAllocation `EFF_BADGE`/`UtilPct`/`MeterBar`/`GaugeRow`/QoS·`text-white` 탭·Recharts hex(712)·축/툴팁 기본색, DailyCheckReview 하위 전 컴포넌트(DeepCheckGrid/TrendChart 범례·라인 hex/DiffPanel/배지/ExecutionStepsTimeline/ResourceTrendChecklist), LAKE `HealthBadge`(고정 emerald/amber/red) | 다크/라이트 톤 어긋남, 상태 판독성 저하 | 높음 | 완료 | 4화면 전부 `--status-*`/`--chart-*` 토큰 전환(K8sManage `38c089e`, 일일점검 `680ed2b`, K8sAllocation `5eab85a`, LAKE `242583e`). LAKE HealthBadge→공용 StatusBadge 래퍼. Recharts 색·축·툴팁 토큰화 |
| D-021 | 에러 상태 | API 실패를 정상 빈/점검 상태로 위장 — K8sManage `OverviewPanel`(578-606) isError 미처리→"노드 0·상태 점검", K8sAllocation `SummarySection`(387-398)·`PodScheduleCalc` isError 무시, DailyCheckReview review/trend isError 미처리→섹션 통째 사라짐 | 백엔드/인증 장애를 정상으로 오판 | 높음 | 완료 | K8sManage OverviewPanel·K8sAllocation SummarySection/PodScheduleCalc·DailyCheckReview review/trend 에 isError 분기 추가 — 빈/점검 위장 제거 (`38c089e`·`5eab85a`·`680ed2b`) |
| D-022 | 인터랙션/안전 | K8sManage 위험 동작이 브라우저 native — scale `window.prompt`(192), restart/delete/drain `window.confirm`(203/211/226). 테마·포커스·검증 UX 없음 | 되돌릴 수 없는 삭제/드레인/스케일이 무검증 native 팝업 — 오조작 위험 | 높음 | 완료 | scale/restart/delete/drain 모두 ConfirmDialog(danger, 대상 kind/ns/name 강조)로 교체, scale 은 다이얼로그 내 정수 입력. cordon/uncordon 은 되돌릴 수 있는 토글이라 즉시 실행 유지 (`d2979b0` 이후 커밋) |
| D-023 | 접근성 | 클릭 요소 키보드 미접근 — K8sAllocation 확장 `<tr>`(1054/1142) role/tabIndex/onKeyDown 없음, LAKE 카드 `<button>` 안에 `<span role=button>` 중첩(LakeServiceCard 60-79), K8sManage DetailDrawer(775) Escape/포커스트랩 없음 | 키보드/스크린리더로 드릴다운·드로어 조작 불가 | 중간 | 완료 | K8sAllocation 확장 tr(`5eab85a`)·LAKE 카드(`242583e`) + K8sManage DetailDrawer 에 `useModalA11y`(Escape·포커스 트랩) 적용. **완료** |
| D-024 | 접근성 | 색 단독 상태 — K8sManage 노드 Ready 점(988)·컨테이너 색칸(1103)이 색만(상태 텍스트는 title 툴팁뿐) | 색맹 사용자가 노드/컨테이너 정상·오류 구분 불가 | 중간 | 완료 | 노드 Ready 점→아이콘+aria-label, 컨테이너 색칸→role=img+aria-label (`38c089e`) |
| D-025 | 반응형 | 다열 테이블 overflow-x 부재(K8sManage Pods/Nodes/Resource grid), K8sAllocation 노드 카드 그리드 고정 열수(853)로 좁은 폭 짓눌림 | 좁은 뷰포트·다컬럼 시 판독 불가 | 중간 | 완료 | K8sManage 테이블 overflow-x-auto(`38c089e`) + K8sAllocation 노드 카드 그리드를 `minmax(min(220px,100%),1fr)`+가로 스크롤로 좁은 폭 대응. **완료** |
| D-026 | 접근성/일관성 | 자체 모달 raw `fixed inset-0`+`bg-black/40`+`rounded-2xl` (25파일/67곳) — Escape/포커스트랩/aria 부재, 테마 미적용 | 키보드로 모달 열고닫기 불가, 배경 포커스 누수 | 중간 | 완료 | **재사용 접근성 훅 `useModalA11y`(Escape·포커스 트랩·초점 이동/복원) 신설** + 공용 `ConfirmDialog`(앱 전역 확인 다이얼로그) 에 적용(role=dialog·aria-modal·aria-labelledby, main 과 독립 토큰화 병합)·운영점검 상세·일일점검 Schedule/Items 모달에 적용. **확산 완료: 폼/다이얼로그형 전용 모달 20종에 `useModalA11y` 부착(수동 Escape effect 제거·role=dialog/aria-modal/aria-labelledby 부여) — 1차 8종(AddLakeServiceModal·AddServiceInstanceModal·ProjectFormModal·ClusterUpdateDiffDialog·ImageDistributeDialog·PlaybookLogDialog·CreateBatchJobWizard·BatchJobSlideOver overlay) + 2차 페이지형/공용(Sprints 2·Users 2·InfraTopology 2(폼+삭제확인)·Settings 2·NodeSpec HostFacts·MindMap NodeEditor·LakeServiceTypeManager·ServiceCategoryManager·공용 SidePane). `useModalA11y` 사용 파일 38→55. 남은 `fixed inset-0` 16곳은 전부 드롭다운/팝오버 click-catcher·터미널(K9s/Pod)·캔버스·Base UI Dialog 프리미티브로 모달 아님(포커스 트랩 미대상). **완료** |
| D-027 | 로딩 상태 | 로딩이 "불러오는 중…" 텍스트뿐 — K8sManage 전 패널·DailyCheckReview 섹션. skeleton 부재로 레이아웃 점프 | 로딩→로드 시 시프트, 상태 인지 지연 | 낮음 | 완료 | K8sManage 에 공용 Skeleton 기반 `RowsSkeleton`(헤더 그리드 아래 행 자리표시) 신설 후 리소스/Nodes/Pods/Helm/CRD(목록·오브젝트) 패널 로더에 적용, 개요 패널은 3-스탯 카드 skeleton, DailyCheckReview AI 리뷰 로더는 텍스트 라인 skeleton 으로 교체. **완료** |
| D-028 | 잔여(낮음) | LAKE 인라인 style 그리드(136/158)·`hover:shadow-md`(22)·버튼 `rounded-lg`(222/231), DailyCheckReview 새로고침/삭제 aria-label 누락·`checkedAt` 툴팁 UTC 원본(129), K8sAllocation hover 전용 툴팁 키보드/터치 미접근(549/582) | 소소한 정합/접근성 이탈 | 낮음 | 완료 | LAKE 인라인 grid/shadow/rounded·일일점검 aria-label/KST(`242583e`·`680ed2b`) + K8sAllocation hover 툴팁을 button+group-focus-within 으로 키보드/터치 접근화. **완료** |
| D-029 | 내비게이션 | **전역 뒤로가기 버튼 부재** — 앱 셸이 사이드바만 있고 공통 헤더/뒤로가기 없음. 상세·폼의 "목록으로"는 고정 경로라 진짜 back 아님, 모달은 브라우저 히스토리 미연동 | 사용자가 이전 화면으로 돌아갈 전역 수단이 없음 (AI 생성 SPA 흔한 취약) | 중간 | 부분완료 | ① 사이드바 로고 하단에 전역 뒤로가기 버튼 — `navigate(-1)`(history.state.idx>0), 딥링크 진입 시 홈 fallback, 홈에선 숨김. ② `useModalA11y` 에 `historyClose` opt-in 추가 — hash 기반(React Router 협조, 인스턴스별 고유 hash+POP 판별로 중첩 안전)으로 **뒤로가기가 모달만 닫도록** 연동, 운영점검 상세·일일점검 Schedule/Items 모달에 적용. ③ **모달 접근성 확산 — 전용 모달 32개에 `useModalA11y`(Escape·포커스 트랩·role=dialog·aria-labelledby) 일괄 부착**(대다수 모달이 Escape 로도 못 닫히던 것 해소). **후속: `historyClose` 를 파일럿 3개 실측 후 확산분에도 일괄 플립, 목록 필터/탭 URL 저장** |
| D-030 | 정보구조/네이밍 | 사용자 제보 3건 — ① Settings "LAKE 타입" 탭명이 "LAKE" 라는 비일반적 용어 사용 ② "관리 서비스"(구 LAKE 타입) 안 "PEP 서비스" 서브탭 콘텐츠(빌트인 8종+카테고리4개, 전부 domain='pep' 하드코딩 시드)가 실제로는 "APP 서비스"에 해당 ③ Settings 최상위 "서비스"(PEP 서비스, `ServiceCatalogManager`)를 "관리 서비스→PEP 서비스" 서브탭으로 이동 | 탭 이름·정보구조가 실제 데이터/의미와 어긋나 혼란 | 중간 | 완료 | ①**완료** — 탭 id `lake-types`→`mgmt-service`, 라벨→"관리 서비스". ②**완료** — PEP=DevOps 관리 인프라(K8s/Cilium/Linux/Keycloak/Nexus/CI-CD/Prometheus/Grafana/AIStor/Network 10종 신규, 평면 목록), APP=사용자 서비스(기존 8종 domain pep→app 재배정 + DataHub 추가, 카테고리 Runtime/Catalog/Workbench/AI Ready). 백엔드 `_seed_default_service_categories` 를 멱등 마이그레이션(domain='pep' 인 것만 1회 전환 → 재시작 강제복원 제거)으로 재작성, 레거시 pep 카테고리 정리. PepServices/AppServices/문서 문구 동기화. ③**완료** — 최상위 "서비스"(PEP 서비스) 탭 제거, `ServiceCatalogManager` 를 "관리 서비스" 탭 내부 서브탭("서비스 타입"=LakeServiceType / "서비스 카탈로그"=ui_settings.serviceCatalog)으로 이동. 레거시 `?tab=service`→mgmt-service 리다이렉트. |
| D-031 | 정보구조/중복 | D-030 후속 사용자 제보 — ① Settings "관리 서비스"와 "서비스 카테고리" 두 최상위 탭이 같은 PEP/APP 도메인을 각각의 도메인 탭으로 중복 노출 ② "관리 서비스" 안 "서비스 타입"/"서비스 카탈로그" 서브탭 구분이 도메인 축과 교차해 3중 축(탭×서브탭×도메인)이 됨 ③ "서비스 카탈로그"(`ui_settings.serviceCatalog`)와 PEP 서비스 타입이 같은 서비스(K8s/Keycloak/Nexus/Prometheus/Grafana/Cilium)를 아이콘·색상까지 각각 정의 | 같은 것을 두 곳에서 관리 — 어디서 고쳐야 하는지 불명확하고 두 정의가 갈라짐 | 중간 | 완료 | ①②**완료** — "서비스 카테고리" 최상위 탭 폐지, "관리 서비스" 탭의 서브탭을 도메인 축(`PEP 서비스`/`APP 서비스`) 하나로 단일화. 각 서브탭 본문 = 카테고리 섹션 + 서비스 타입 섹션. `ServiceCategoryManager`/`LakeServiceTypeManager` 가 `domain` prop 을 받고 자체 도메인 탭·도메인 select 제거(단일 출처). 레거시 `?tab=service-categories`→mgmt-service 리다이렉트. ③**완료** — 아이콘/색상 정의를 PEP 서비스 타입으로 일원화: `lake_service_types.color` 컬럼 추가, `useServiceCatalog()` 가 `ui_settings` 대신 LakeServiceType(domain=pep) 을 조회, 부팅 시 `_merge_service_catalog_into_pep_types()` 로 겹치는 6종 color 백필 + 카탈로그 전용 6종(jenkins/argocd/etcd/hubble/ingress/storage) PEP custom 타입 이관, `ui_settings.service_catalog` 필드·`ServiceCatalogManager` 폐기 |

### R-4 3차 라운드 발견 (클러스터 정보 수정 `/cluster-manage/:id/edit` — 2026-07-25)

`ClusterMetaFormPage.tsx`(433행) 단일 화면 정성 리뷰 + 백엔드(`routers/clusters.py` `update_cluster`,
`schemas/cluster.py` `ClusterUpdate`) 대조. 사용자 요청("UX/UI 디자인 및 기능 버그 개선 점검").

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-031 | 기능 버그 | **입력값을 지워서 저장할 수 없음** — `handleSubmit`(120-146)이 빈 문자열을 전부 `undefined` 로 직렬화(`region.trim() \|\| undefined` 등 22개 필드). `JSON.stringify` 가 `undefined` 키를 제거하고, 백엔드 `update_cluster` 는 `model_dump(exclude_unset=True)`(412행)라 **해당 필드를 아예 미수신 → 기존 값 유지**. 화면상으론 지워졌는데 저장 후 되돌아옴 | 잘못 입력한 CIDR/MAC/호스트명/설명을 **해제할 방법이 없음**. v1.13.0 에서 Batch Jobs 편집 폼에 대해 고친 것과 **동일한 버그 클래스** | 높음 | 완료 | 빈 입력을 `orNull()` 로 `null` 전송하도록 교체(24필드) + `ClusterManageUpdate` 타입에 `\| null` 반영. 요청 인터셉터 `convertKeysToSnake` 가 `null` 은 보존하고 `undefined` 만 유실되는 것, 대상 컬럼 24개가 전부 `nullable=True` 인 것 확인. 대조군: `bgpEnabled`/`prometheusEnabled` 는 boolean 이라 원래도 정상 동작 — 진단 확증 |
| D-032 | 기능 버그 | **로딩 상태 미처리로 입력 유실** — `useClusters()`(24행) 반환값을 버려 `isLoading`/`isError` 미사용. 스토어가 비면 `cluster` 가 undefined 인 채 **빈 폼이 그대로 렌더**되고(찾을 수 없음 분기는 `clusters.length > 0` 조건), 사용자가 입력하는 도중 목록이 도착하면 `hydrated=false` 라 hydration effect(62-90)가 **입력값을 서버값으로 덮어씀**. 이 시점 저장 버튼은 `if (!cluster) return`(116행)으로 **무반응** | 딥링크·느린 API 진입 시 입력이 소리 없이 사라지고 저장이 먹통. 조회 실패해도 에러 없이 빈 폼만 표시 | 높음 | 완료 | `useClusters()` 의 `isLoading`/`isError`/`refetch` 를 사용해 **로딩/조회실패/미발견 3분기** 신설 — 데이터 확정 전에는 폼 대신 폼 구조를 흉내낸 skeleton 을 렌더하므로 입력 유실·저장 무반응 경로가 사라짐. 조회 실패 시 "다시 시도" + 목록 이동 제공 |
| D-033 | 기능/에러 | **저장 실패 사유 은폐 + 입력 검증 부재** — `catch { setError('저장에 실패했습니다...') }`(150-152)가 실제 API 오류를 폐기(앱 공용 `formatApiError` 미사용). 동시에 CIDR/IP/MAC/AS Number 가 전부 자유 텍스트로 `pattern`·형식 검증 없음 | 잘못된 CIDR 로 422 를 받아도 무엇이 왜 틀렸는지 알 수 없어 수정 불가 | 높음 | 완료 | `formatApiError(err, fallback)` 로 교체해 백엔드 detail 노출 + **제출 시 형식 검증 15필드**(CIDR 3·IPv4 6·IP\|CIDR 2·MAC 2·AS Number) 신설 — 값이 있을 때만 검사(빈 값=해제는 통과), 실패 필드는 `aria-invalid`+테두리 강조+인라인 사유, 오류가 있는 첫 필드의 탭으로 자동 전환. 검증 규칙은 앱 기존 `parseCidrRange`(겹침 검사)와 동일 형식이라, 통과하는 값 = CIDR 계산기가 인식하는 값 |
| D-034 | 디자인 토큰 | **고정 팔레트 8곳** — N/W CIDR 탭 3개 섹션(`border-sky-500/20 bg-sky-500/5 text-sky-600`, `emerald-500`+`text-emerald-400`, `violet-500`+`text-violet-400`)과 기타 탭 Prometheus 섹션(`cyan-500`). 특히 `text-emerald-400`/`text-violet-400` 은 **다크 전용 톤**이라 default/light 테마에서 대비 부족 | 3테마 전환 시 이 화면만 톤이 어긋나고 섹션 라벨 판독성 저하 | 높음 | 완료 | 네트워크 도메인 3종(INTERNAL_IP/Pod/Service)+Prometheus 섹션을 categorical `chart-1~4` 토큰으로 전환(`border-chart-N/20 bg-chart-N/5 text-chart-N`) — 범주 구분이지 의미색이 아니므로 status 가 아닌 chart 계열 선택. 고정 팔레트 스캔 8건→**0건** |
| D-035 | 일관성 | **MacCard 미사용 + 라운딩/버튼 컨벤션 이탈** — 폼 전체가 손수 만든 `bg-card border border-border rounded-xl`(177행) 카드(CLAUDE.md D-004 금지 규칙), 입력·버튼 13곳이 `rounded-lg`(표준: 버튼/입력 `rounded-xl`, 카드 `MacCard`=`rounded-md`), 취소/저장이 shadcn `Button` 대신 raw `<button>` | 다른 화면과 카드 표면·모서리·버튼 높이가 어긋나 전환 시 점프 | 중간 | 완료 | 손수 만든 카드 div → `MacCard`(`bodyPadding="p-0"`) 전환, 입력 `rounded-lg`→`rounded-xl`(버튼/입력 표준)·내부 그룹 박스→`rounded-md`(테마 인지)로 정리, 취소/저장/재시도/뒤로가기를 shadcn `Button`(secondary/ghost/icon)으로 교체. `rounded-lg` 잔여 **0건**, raw `<button>` 은 자체 밑줄 스타일이 필요한 탭 1곳만 유지 |
| D-036 | 접근성 | **탭 ARIA 전무 + 에러 미고지** — 탭(200-219)에 `role="tablist"/"tab"/"tabpanel"`·`aria-selected`·좌우 화살표 이동 없음(그냥 `<button>` 3개), 에러 배너(222-226)에 `role="alert"` 없고 포커스 이동도 없음, `text-[10.5px]`(314행)은 임의 소수점 폰트크기 | 스크린리더가 탭 구조·저장 실패를 인지 못함, 11px 미만 텍스트 판독성 저하 | 중간 | 완료 | 탭에 WAI-ARIA tabs 패턴 적용 — `role=tablist/tab/tabpanel`·`aria-selected`·`aria-controls`/`aria-labelledby`·roving tabindex + ←/→·Home/End 키 이동. 에러 배너에 `role="alert"` 부여(저장 실패·검증 오류가 스크린리더에 즉시 고지), `text-[10.5px]`→`text-xs`. 임의 폰트크기 **0건** |
| D-037 | 데이터 안전 | **미저장 변경 이탈 경고 없음 + 저장 성공 피드백 없음** — 22개 필드 3탭 장문 폼인데 취소/뒤로가기/사이드바 이동 시 경고 없이 소실. 저장 성공 시 toast 없이 목록으로 즉시 이동(앱 공용 `useToast` 미사용) | 실수로 이탈하면 입력 전체 소실, 저장이 됐는지 확신 불가 | 중간 | 완료 | 기준 스냅샷(키 정렬 직렬화) 대비 dirty 추적 신설 → 취소·뒤로가기 버튼에 `ConfirmDialog`(danger, "계속 편집"/"나가기"), 새로고침·탭 닫기는 `beforeunload` 경고. 저장 성공 시 `toast.success` 추가. **한계: 앱이 선언형 `BrowserRouter` 라 `useBlocker` 를 쓸 수 없어 사이드바 등 앱 내 링크 이동은 차단 대상이 아님** — 데이터 라우터 전환 시 확장 가능(별도 과제) |
| D-038 | 상태/URL | **활성 탭이 URL 에 없음** — `useState<TabId>('node')`(56행). 새로고침·공유·뒤로가기 시 항상 `노드 스펙` 탭으로 복귀 | 특정 탭을 공유/북마크 불가, 저장 실패 후 새로고침하면 작업 탭 잃음 | 중간 | 완료 | `useSearchParams` 로 `?tab=node\|network\|extra` 영속화(알 수 없는 값은 `node` 로 폴백). 탭 전환은 `replace: true` 라 히스토리에 쌓이지 않아 **전역 뒤로가기가 탭 이동을 되짚지 않는다**(D-029 연계). **D-029 후속(목록 필터·탭 URL 저장)의 첫 적용 사례 — 다른 화면 확산 시 이 패턴을 참조** |
| D-039 | 기능 버그(잠재) | **`hydrated` 가 클러스터 변경에 반응하지 않음** — 라우트(`App.tsx` 158행)에 `key` 가 없어 `/cluster-manage/A/edit` → `/cluster-manage/B/edit` 이동 시 컴포넌트가 재사용되고 `hydrated=true` 가 남아 **A 의 값이 표시된 채 B 를 저장**할 수 있음 | 브라우저 앞/뒤 이동·URL 직접 편집 경로에서 타 클러스터 값 덮어쓰기 위험 | 중간 | 완료 | `hydrated` boolean → `hydratedId`(하이드레이션한 클러스터 id)로 교체해 `cluster.id` 가 바뀌면 재하이드레이션 + 검증오류·기준 스냅샷 초기화. 같은 id 의 30초 자동 리페치에서는 조기 반환하므로 **편집 중 입력이 덮어써지지 않는 성질은 그대로 유지**됨(회귀 확인) |
| D-040 | 문서 정합 | **SCREENS.md 가 존재하지 않는 기능을 기술** — "클러스터 정보 수정" 절(560·563·569행)이 Coroot APM 연동(`coroot_project`/`coroot_url`/`coroot_enabled` 매핑·토글)을 명시하지만, 해당 필드가 **백엔드 모델·스키마·프론트 타입·화면 어디에도 없음**(grep 0건) | 문서를 믿고 접근하는 후속 작업자가 없는 기능을 찾게 됨 | 낮음 | 완료 | SCREENS.md 에서 Coroot 기술 3곳 제거(목적/Backend 컬럼 목록/핵심 기능). 아울러 이번 라운드로 바뀐 화면 구조(MacCard·탭 ARIA·chart 토큰·로딩/에러 분기·null 해제·검증 15필드)를 같은 절에 현행화 |

### R-4 4차 라운드 발견 (클러스터 관리 목록 `/cluster-manage` — 2026-07-29)

`ClusterManagePage.tsx`(687행) + `components/cluster-manage/*`(11개 · 총 2,300여 행) 정성 리뷰.
백엔드 `routers/clusters.py`(`reorder_clusters`/`update_cluster`), `hooks/useCluster.ts`,
`hooks/useClusterCustomFields.ts`, `services/api.ts` 요청 인터셉터와 대조. 사용자 요청
("클러스터 관리 화면 레이아웃 점검 및 버그 audit"). **코드 변경 없음 — 발견 등재만.**

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-041 | 기능 버그 | **인라인 편집으로 값을 지울 수 없음 (D-031 과 동일 클래스가 목록 화면에 잔존)** — `ClusterTableRow.quickUpdate`(75-77)가 `{ region: v \|\| undefined }` 형태로 5개 필드(지역·운영레벨·INTERNAL_IP·Pod/Svc CIDR)를 전송. 요청 인터셉터 `convertKeysToSnake` 는 키를 보존하지만 `JSON.stringify` 가 `undefined` 를 제거 → 백엔드 `update_cluster` 의 `model_dump(exclude_unset=True)` 가 **미수신으로 판단해 기존 값 유지** | 표에서 잘못된 CIDR/지역을 비워 저장하면 화면상 지워졌다가 다음 리페치(30초)에 되살아남. 편집 폼(`:id/edit`)에서는 D-031 로 이미 고쳤는데 목록 경로만 남음 | 높음 | 완료 | `quickUpdate` 5개 필드를 `v.trim() \|\| null` 전송으로 교체, `useUpdateCluster`/`clustersApi.update` 시그니처에 `ClusterManageUpdate`(null 허용) 반영. 대상 컬럼 5개 모두 `nullable=True` 확인 |
| D-042 | 기능 버그 | **셀 저장 실패가 조용히 사라짐 + 낡은 draft 덮어쓰기** — ① `useUpdateCluster`(hooks/useCluster.ts)에 `onError` 없음, 호출부도 `onSettled` 로 편집만 닫음 → 422/403 이어도 토스트 없이 값이 원복 ② `ClusterCustomCell.commit`(28-34)은 `mutateAsync` 를 try/catch 없이 `onBlur` 에서 호출 → 실패 시 **unhandled rejection**, 사용자는 저장된 줄 앎 ③ 같은 컴포넌트 `draft` 가 lazy initializer 1회 계산이라, 자동수집·타 사용자 변경으로 서버 값이 바뀐 뒤 다시 편집하면 **옛 값으로 되돌려 저장** | 편집이 실패해도 성공한 것처럼 보이고, 커스텀 컬럼은 최신 값을 덮어쓸 수 있음 | 높음 | 완료 | `ClusterTableRow` mutate 호출부에 `onError`+`formatApiError` 토스트(훅 공통이 아닌 호출부 처리 — SettingsPage 등 자체 인라인 에러와 이중 고지 방지), `ClusterCustomCell.commit` try/catch(실패 시 편집 유지·재시도 가능), `beginEdit` 로 편집 진입 시점 서버 값으로 draft 초기화 |
| D-043 | 로딩/에러 상태 | **로딩·조회실패를 "클러스터 없음" 으로 위장 (D-032·D-021 동일 클래스)** — 페이지가 `useClusters()`(86행) 반환값을 버려 `isLoading`/`isError` 미사용. `clusterStore` 초기값이 `[]` 라 최초 진입·API 실패 시 빈 배열 분기(509-514)가 그대로 걸려 **"등록된 클러스터가 없습니다 · Settings 에서 먼저 등록하세요"** 를 표시 | 백엔드/인증 장애를 "등록된 클러스터가 없다" 로 오판해 중복 등록을 시도하게 됨 | 높음 | 완료 | `useClusters()` 의 `isLoading`/`isError`/`error`/`refetch` 사용 — 로딩=표 구조 skeleton(`SkeletonTable`), 실패=`formatApiError` 사유+다시 시도 버튼, 진짜 0건일 때만 빈 상태 (D-032 3분기 패턴 준용) |
| D-044 | 기능 버그 | **검색/필터 상태에서 드래그하면 숨겨진 클러스터의 순서가 오염됨** — `handleDragEnd`(211-238)가 `groupedClusters`(= `filteredClusters` 파생, 검색어·운영레벨 필터 적용됨)만으로 `fullOrder` 를 만들어 전송. 백엔드 `reorder_clusters` 는 **받은 id 에만 `seq = 1000 + i*10` 을 재할당**하므로 필터로 가려진 클러스터는 옛 seq 를 유지 → 보이는 것들이 숨은 것들 사이로 끼어듦 | 필터를 켠 채 카드 한 장만 옮겨도 필터 해제 시 전체 순서가 뒤죽박죽. 되돌릴 방법은 수동 재정렬뿐 | 높음 | 완료 | ②안 채택 — `fullOrder` 를 전체 `clusters`(스토어 순서=seq 정렬) 에서 `arrayMove(fullIds, from, to)` 로 재구성. 이동 대상 외 상대 순서가 보존되므로 가려진 클러스터 자리 유지. 비수동 정렬 중 드래그도 이전(정렬 순서로 seq 전체 재작성)보다 덜 파괴적이 됨 — 드래그 게이팅 자체는 D-045 에서 |
| D-045 | 인터랙션 정합 | **정렬 모드와 드래그가 어긋남** — ① `sortBy` 가 `name/status/level` 일 때도 드래그가 허용돼 `seq` 는 바뀌지만 화면은 즉시 재정렬돼 **드롭이 되돌아온 것처럼** 보임 ② 드래그는 카드 뷰에만 있는데 기본 정렬 옵션 이름은 "수동(드래그)" 이고 **테이블 뷰엔 드래그 수단이 없음** ③ 페이지 정렬은 `seq ?? 0`, `useClusters` 는 `seq ?? 1000` — seq 미할당 클러스터의 위치가 두 기준에서 반대 | 순서 변경이 "먹지 않는다" 고 느끼고, 테이블 사용자는 수동 정렬 수단을 못 찾음 | 중간 | 완료 | ①드래그·핸들·`useSortable` 을 `sortBy==='manual'` 에서만 활성(`sortEnabled`), `handleDragEnd` 에도 안전 가드 ②테이블 뷰에 행 드래그 추가 — 페이지 `DndContext`+그룹별 `SortableContext`(`verticalListSortingStrategy`), `ClusterTableRow` 가 `sortable` prop 으로 `useSortable` 사용, 이름 셀 좌측 hover/focus 노출 그립(`aria-label` 병행) ③`filteredClusters` 수동 정렬 fallback 을 `seq ?? 1000` 으로 `useClusters` 와 통일 |
| D-046 | 기능 버그 | **이름 표준화 모달이 30초마다 입력을 초기화** — `StandardizeClusterNamesModal` 의 `useEffect(..., [open, clusters])`(37-42)가 `clusters` **참조**에 반응. `useClusters` 가 30초 폴링마다 `setClusters(새 배열)` 을 호출하므로 모달이 열려 있는 동안 주기적으로 `edits`/`doneIds` 가 리셋됨 | 여러 클러스터 이름을 다듬는 도중 입력과 완료 표시가 통째로 날아감(30초 안에 끝내야 함) | 중간 | 완료 | 초기화 effect 재구성 — 닫힐 때 `edits`/`doneIds` 를 비우고, 열려 있는 동안의 재실행(리페치)은 `setEdits(prev => ...)` 머지로 사용자가 만진 항목을 보존한 채 새/삭제 항목만 반영. 의존성 배열은 그대로 두어 exhaustive-deps 준수 |
| D-047 | 기능 버그 | **동시 auto-update 레이스** — `autoUpdateAbortRef`(115)와 `autoUpdatingId` 가 단일 슬롯. A 수집 중 B 를 누르면 A 의 AbortController 가 덮여 **A 는 중지 불가**, 먼저 도착한 응답의 `finally`(315-318)가 진행 중인 쪽의 `autoUpdatingId`/ref 를 지워 **B 의 스피너·중지 버튼이 사라지고**, diff 다이얼로그는 나중 응답으로 덮어써짐 | 여러 클러스터를 연달아 수집할 때 어느 것이 진행 중인지 알 수 없고 중지가 먹지 않음 | 중간 | 완료 | 진행 상태를 `Set<clusterId>`(state) + `Map<clusterId, AbortController>`(ref) 로 전환 — 행/카드는 `autoUpdating: boolean` prop 을 받아 클러스터별 스피너·중지 독립 동작. diff 는 `openDiff`/`closeDiff` 헬퍼+`diffClusterRef` 로 열린 대상을 추적해, **다른 클러스터의 늦은 응답이 열린 다이얼로그를 덮지 않고** `toast.info` 로 안내(엉뚱한 대상 적용 방지). 수집 실패 토스트에 클러스터명 표기 |
| D-048 | 안전/위험동작 | **되돌릴 수 없는 동작이 표준 게이트를 안 거침 (D-022·D-014 미적용)** — ① 클러스터 삭제가 브라우저 native `confirm()`(281)인데 실제로는 Addon·Playbook·점검 이력까지 캐스케이드 삭제 ② "노드 IP 일괄 수집"(339-359)은 확인 없이 **모든 미수집 클러스터에 dryRun 없는 auto-update 를 순차 적용**(이름과 달리 hostname/CIDR/버전까지 갱신), 취소 불가·진행률 없음, `fail` 이 전건이어도 `toast.success('일괄 수집 종료')` | 오조작 시 이력까지 소실되고, 일괄 수집은 무엇이 얼마나 바뀌는지 모른 채 N개 클러스터를 변경 | 높음 | 완료 | 삭제→`ConfirmDialog`(danger, Addon·Playbook·점검 이력 연쇄 삭제 명시), 일괄 수집→실행 전 확인(대상 수·이름 미리보기·갱신 범위 명시)+진행률 `n/N` 버튼 표기+재클릭으로 중단+결과 토스트 4분기(완료/중단/부분 실패 warning/전건 실패 error) |
| D-049 | 디자인 토큰 | **고정 팔레트 115곳** — `constants.ts` 47(`STATUS_STYLE`·`LEVEL_BADGE`·`OVERLAP_COLORS`), `ClusterTableRow` 23, `IpGroupRow` 16, `ClusterCard` 14, 페이지 6 등. 더 나쁜 건 **톤이 뒤섞인 것**: `text-*-400/300`(다크 전용)과 `text-cyan-700`/`text-amber-700`(bond0/bond1 열, 라이트 전용)이 한 화면에 공존해 **어느 테마에서도 한쪽이 깨짐**. 헤더 카운트 배지도 `bg-slate-500/15 text-slate-400` | 3테마 전환 시 이 화면만 톤이 어긋나고, 다크에서 bond0/bond1 IP, 라이트에서 상태 배지가 저대비 | 높음 | 완료 | 스캔 **115건 → 0건**. `constants.ts` 의 `STATUS_STYLE`→`status-*`(dot/border-l/badge), `OVERLAP_COLORS`→categorical `chart-*` 5색, 미사용 `OPERATION_LEVELS`/`LEVEL_BADGE`(참조 0건, 운영레벨은 Settings 기반 `useOperationLevels` 로 이관됨) 제거. TableRow 23건(BGP·cilium→chart-6, bond0→chart-6, bond1→chart-3, k8s→chart-1, public IP·겹침→status-warning, 삭제/중지→status-critical), `IpGroupRow` ACCENT 5종→chart 토큰, Card·CustomCell·모달 4종·페이지 배지까지 전환. 같은 화면에서 렌더되는 `NodeNicsCollectModal`(versions 공용)도 함께 처리. **범위 밖으로 남긴 것: `hooks/useOperationLevels.ts` 의 `COLOR_BADGE`** — 다른 화면 다수가 공유하는 사용자 지정 색 매핑이라 전 화면 영향 검토가 필요(별도 과제) |
| D-050 | 일관성 | **MacCard 미사용 (D-004 금지 규칙)** — 검색/필터 패널(457)·테이블 래퍼(521)가 수제 `bg-card border border-border rounded-xl`, `ClusterCard` 도 수제 `rounded-xl`+`hover:shadow-md` 카드. `pages/*.tsx` 43개가 MacCard 를 쓰는데 이 화면은 **0건** | 표면·모서리·그림자가 다른 화면과 어긋나 전환 시 점프 | 중간 | 완료 | 검색/필터 패널→`MacCard title="검색 / 필터"`, 표 컨테이너·로딩 skeleton→`MacCard bodyPadding="p-0"`, `ClusterCard` 는 `rounded-xl`+`shadow-sm hover:shadow-md`→`rounded-md`(테마 인지)+`mac-shadow`(테마 토큰), 필터 초기화 버튼 `rounded-lg`→`rounded-xl`. 페이지 `MacCard` 사용 0건→3곳 |
| D-051 | 레이아웃/반응형 | **표·헤더가 좁은 폭과 리사이즈를 견디지 못함** — ① 페이지 헤더(384-453)가 `justify-between` 한 줄에 ViewModeBar+버튼 5개, `flex-wrap` 없음 → 좁은 폭에서 제목/배지와 충돌 ② `thead` 가 sticky 아님 → 13열+커스텀열 표를 세로로 내리면 헤더 소실 ③ `tableLayout:'fixed'`+colgroup 인데 `td/th` 에 `overflow-hidden`/`truncate` 부재 → **컬럼을 좁히면 내용이 옆 칸으로 넘쳐** 리사이즈 기능이 사실상 안전하지 않음 ④ 카드 그리드가 `minmax(320px,1fr)` 고정(D-025 에서 정리한 `minmax(min(320px,100%),1fr)` 미적용) ⑤ 카드 드래그 핸들(`absolute top-2 left-2 z-10`)이 헤더의 상태 점·운영레벨 아이콘을 덮음 | 노트북/분할 화면에서 헤더가 짓눌리고, 긴 클러스터명·호스트명이 이웃 열을 침범하며, 스크롤 시 어느 열인지 알 수 없음 | 중간 | 완료 | ①헤더 좌/우 블록 모두 `flex-wrap`+`gap-3` ②`DoubleScrollX` 에 `bodyClassName` opt-in prop 신설 → 본문 `max-h-[calc(100vh-16rem)]` 로 세로 스크롤을 만들고 `thead sticky top-0 z-10 bg-secondary`(solid) 로 고정. `border-collapse` 에서 sticky 셀 border 가 사라지므로 구분선은 `shadow-[inset_0_-1px_0_hsl(var(--border))]` ③모든 `td` 에 `overflow-hidden`(th 는 `ResizeGrip` 이 음수 마진으로 걸쳐 있어 제외) + 이름/호스트명 `truncate`+`title` — 컬럼을 좁혀도 이웃 열을 침범하지 않음 ④카드 그리드 `minmax(min(320px,100%),1fr)` ⑤카드 드래그 핸들은 위치 유지하되 **수동 정렬에서만 노출**(D-045)돼 상시 충돌은 해소 — 완전한 위치 이동은 카드 헤더 재배치가 필요해 보류 |
| D-052 | 접근성 | **편집·정렬 진입점이 마우스 전용** — ① `EditableCell`(35-68)이 `onDoubleClick` 전용으로 `role`/`tabIndex`/`onKeyDown` 없음, 진입 버튼은 `opacity-0 group-hover:opacity-100` 뿐이라 **포커스해도 보이지 않음**(`focus-visible:opacity-100` 부재) — 카드 드래그 핸들도 동일 ② 아이콘 전용 버튼 대부분이 `title` 만(대상 파일 `title` 40여 곳 대비 `aria-label` 1~2곳) — `ClusterCard` 4개(수집/NIC/수정/삭제)는 `aria-label` **0건**, 12.4 표준 위반 ③ 그룹 헤더가 🌐/🏷️ 이모지로만 종류 구분, 커스텀 컬럼 헤더는 `title=""`(빈 문자열) | 키보드/스크린리더 사용자가 인라인 편집·순서 변경에 접근 불가, 버튼 목적 판독 불가 | 중간 | 완료 | ①편집 진입 버튼(연필)에 `focus-visible:opacity-100`+`focus-visible:ring` — 이미 실제 `<button>` 이므로 **탭 순서에는 있었지만 보이지 않던 것**을 해소(td 에 `role="button"` 을 덧씌우는 대신 셀 안 버튼을 키보드 경로로 확정, `focus-within:bg-primary/5` 로 셀 강조). 행 드래그 그립도 동일 처리 ②아이콘 전용 버튼 `aria-label` 일괄 부여 — TableRow 5개(재수집/중지·NIC·수정·삭제·Cilium 설정)·Card 4개, 전부 클러스터명 포함(`"prod-a 삭제"`)해 행/카드가 많아도 대상 식별 가능 ③그룹 헤더 이모지(🌐/🏷️)→lucide `Globe`/`Tag`(`aria-hidden`)+텍스트 라벨("지역"/"운영레벨") ④커스텀 컬럼 헤더 빈 `title=""`→`커스텀 컬럼: {label}` 폴백, `ⓘ` 장식에 `aria-hidden` |
| D-053 | 잔여(낮음) | ① 검색/필터/정렬/그룹/뷰모드가 **URL 미저장** — 새로고침·공유 시 초기화(D-029 후속 "목록 필터 URL 저장" 대상, D-038 패턴 준용 가능) ② `parseCidrRange`(39-49)가 옥텟 범위를 검사하지 않아(`\d{1,3}`) `999.1.1.1/24` 같은 값도 겹침 판정에 참여, 헤더 배지 "CIDR 겹침 N건" 의 N 은 실제로 **겹침에 연루된 클러스터 수**(쌍 수 아님) ③ 커스텀 컬럼 `<td style={{width}}>`(ClusterTableRow 474)가 colgroup·사용자 리사이즈와 중복돼 사문 ④ 빈 상태/검색 무결과가 공용 `EmptyState` 미사용이고 Settings 등록 화면으로 가는 **링크 없이 문구만** 안내 | 소소한 정합·판독성 이탈 | 낮음 | 완료 | ①`useSearchParams` 로 `?q=&level=&sort=&group=&view=` 영속화 — 기본값은 URL 에서 생략, `replace: true` 라 히스토리 미적재(D-038 패턴), 알 수 없는 값은 기본값 폴백, 필터가 URL 에 있으면 패널을 펼친 상태로 진입 ②`parseCidrRange` 에 옥텟 0–255 검증 추가(잘못된 값이 겹침 판정에 참여하던 것 차단), 배지 문구를 "CIDR 겹침 클러스터 {N}개"(실제 값=연루 클러스터 수)로 정정+설명 `title` ③커스텀 컬럼 `td` 의 사문 `style={{width}}` 제거(colgroup·사용자 리사이즈가 실제 결정) ④빈 상태/조회 실패/검색 무결과를 공용 `EmptyState` 로 통일 + **"Settings 에서 클러스터 등록"(`/settings?tab=clusters`)·"다시 시도"·"필터 초기화" CTA** 부여 |

### R-4 5차 라운드 발견 (홈 모드 분기·전역 네비게이션 IA — 2026-07-29)

사용자 제보("업무 현황/클러스터 관리 모드 분기가 오히려 혼란") 기반 IA·네비게이션 감사.
`HomePage.tsx`·`Sidebar.tsx`·`navConfig.ts`·`homeStore.ts`·`authStore.ts`·`useNavCatalog.ts` 대조.
클릭 수 실측 + 3-click rule/Hick's law/information scent/일관성 기준 평가. **코드 변경 없음 — 등재만.**

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-054 | 정보구조 | **홈 모드(work/platform)가 사이드바 네비게이션 전체를 게이팅** — `Sidebar.visibleGroups`(267-270)가 `GROUPS.modes` 로 필터해, work 모드에선 클러스터/서버/네트워크/스토리지/DevOps 그룹(36개 화면)이, platform 모드에선 협업 그룹(7개 화면)이 레일에서 완전히 사라짐. 반대 모드 기능은 "존재한다는 단서(정보 냄새)" 자체가 0 | 임의 화면에서 반대 도메인 화면 진입이 최소 4클릭(로고→홈, 로고→토글, 레일, flyout). 신규 사용자는 "메뉴가 없어졌다"고 오인 — 사용자 제보의 직접 원인 | 높음 | 완료 | 개선안 B(사이드바 통합+홈 탭) 채택. `GROUPS.modes`(게이팅) → `GROUPS.domain`(배치만 결정)로 의미 전환 — `platform` 은 좌측 사이드바, `work`(협업/문서 관리)는 신규 전역 `components/layout/AppTopBar.tsx` 로 이동, `system` 은 레일 하단. `Sidebar.visibleGroups` 는 `domain==='platform'` 만 필터하고 게이팅은 완전히 제거 — 두 도메인이 항상 동시에 보여 반대 도메인 진입이 어느 화면에서든 1~2클릭 |
| D-055 | 발견성/일관성 | **모드 전환 수단이 로고 버튼의 숨은 토글 하나뿐 + 위치 의존 이중 동작** — `handleHomeClick`(192-199): 홈에서는 모드 토글, 다른 화면에서는 홈 이동. 라벨 없는 아이콘 버튼 하나가 브랜드/홈/모드전환 3역할. 전환 존재는 hover 툴팁으로만 학습 가능 | 같은 버튼이 상황마다 다르게 동작(일관성 위반)해 모드 전환을 발견 못 하거나, 홈 가려다 의도치 않게 화면이 통째로 바뀜 | 높음 | 완료 | D-054 로 모드 자체가 폐지되며 자연 해소 — `handleHomeClick` 은 위치 무관 `navigate('/')` 단일 동작으로 단순화. 홈 화면 안의 선택(내 업무/플랫폼 현황)은 `HomePage.tsx` 의 라벨 있는 세그먼트 탭(`role="tablist"`, 좌우 화살표 키 이동)으로 대체 — 사이드바/네비 노출 범위와 무관한 순수 홈 로컬 상태 |
| D-056 | 상태 지속성 | **로그인마다 `setMode('work')` 강제 리셋** (`authStore.ts:66-68`) — localStorage 에 모드를 저장해 놓고 로그인 시 무효화 | 플랫폼 현황을 주 화면으로 쓰는 운영자가 매 세션 수동 재전환 — 개인 선호가 유지되지 않음 | 중간 | 완료 | `authStore.setSession()` 의 강제 리셋 호출 제거. `homeStore.ts` 의 `mode`→`homeTab` 개명과 함께 localStorage 키도 `pep:homeMode`→`pep:homeTab` 로 이전(구 키 1회 마이그레이션, 기존 사용자 선호 보존) |
| D-057 | 정보구조 | **지식 도메인 라우트 8개가 사이드바 고아** — `/docs`(지식 허브)·`/work-guides`·`/ops-notes`·`/mindmap`·`/ontology`·`/trends`·`/k8s-logs`·`/jira-import` 가 NAV_MAP 에는 있으나 어떤 GROUPS 에도 미포함. 진입로가 URL 직접 입력·Your Island 등록·산발적 컨텍스트 링크뿐 | CLAUDE.md 가 정의한 4대 축(모니터링/인프라/협업/**지식**) 중 지식 축이 네비게이션에서 실종 — 클릭 수 측정 불가(도달 불가) | 높음 | 완료 | `/k8s-logs` 를 `cluster` 그룹에 편입. `/docs`·`/work-guides`·`/ops-notes`·`/mindmap`·`/ontology`·`/trends` 는 이미 별도 커밋으로 "문서 관리" 그룹에 복귀돼 있었음(2026-07). 마지막 잔여였던 `/jira-import`(`JiraImportModal`에서 진입하는 Jira Excel 가져오기 화면)를 `collab`(협업) 그룹에 편입해 고아 라우트 0건 |
| D-058 | 선택지 과부하 | **'클러스터' 그룹 flyout 이 20개 항목 단일 플랫 리스트** (`navConfig.ts:80`) — 모니터링(현황/추이/이벤트/알람)·콘솔(k9s/SSH/etcdctl/일괄실행)·점검(운영점검/리뷰/항목관리)·관리(클러스터 관리/버전) 가 구분 없이 나열. 반면 work 모드는 실질 그룹 1개(협업 7) — 모드 간 밀도 극단 불균형 | Hick's law — 20지선다 스캔으로 목표 항목 탐색 시간 증가, 매 진입마다 반복 | 중간 | 완료 | `Sidebar.renderFlyoutBody`가 `cluster` 그룹만 `CLUSTER_FLYOUT_SECTIONS`(모니터링/콘솔/점검/관리 4개 소제목)로 재배열해서 보여준다. `navConfig.ts`의 `GROUPS.cluster.paths` 자체(단일 소스)는 그대로 두고 렌더링만 나눈다 — 섹션 목록에 없는 새 경로는 "기타" 섹션으로 떨어져 조용히 숨지 않는다 |
| D-059 | 일관성 | **레일 버튼 동작 비일관 + 서비스 도메인 모드 분산** — ①paths 1개 그룹(PEP/APP 서비스, 서비스/앱)은 클릭 즉시 이동, 2개 이상은 flyout — 시각 구분 없음 ②서비스 관련 그룹이 '서비스/앱'(platform, /lake-services)과 'PEP/APP 서비스'(work)로 두 모드에 분산 | 어떤 아이콘이 열리고 어떤 것이 이동인지 예측 불가, 서비스 정보를 찾는 위치가 모드에 따라 달라짐 | 중간 | 완료 | ②는 PEP/APP 서비스 완전 삭제(2026-08-05)로 자연 해소 — 서비스 관련 그룹은 이제 `services`(`/lake-services`) 하나뿐. ①은 `RailIconButton`에 `hasFlyout` prop 을 추가해 하위 경로 2개 이상인 아이콘에만 작은 점 인디케이터를 붙여 즉시 이동 아이콘과 시각적으로 구분 |
| D-060 | 첫 화면 가시성 | **랜딩(work 모드) 위 폴드에 플랫폼 상태가 KPI 필 1개뿐** — 로그인 직후(work 강제) 클러스터 건강 정보는 "위험 클러스터 N/M" 필 하나. 점검 매트릭스 요약·최근 실패 항목 없음. 역으로 platform 모드에선 업무 정보가 KPI 필뿐. 즐겨찾기/최근 방문 진입 수단도 부재(Your Island 는 수동 구성 필요) | 운영 도구인데 첫 화면에서 "지금 플랫폼이 괜찮은가"를 확 인지할 수 없음 — 모드를 오가며 확인해야 함 | 중간 | 완료 | D-054 로 홈 통합(개선안 B)이 이미 자연 해소(KPI 스트립이 탭과 무관하게 항상 노출). `useCheckMatrixFailureCount()` 기반 "점검 실패" KPI 필 + 플랫폼 탭 배지(위험 클러스터+점검 실패 합계, 0건이면 숨김) 신설. 즐겨찾기(서버 저장 `home_prefs.pinnedPaths`, `AppTopBar`★ 드롭다운 + 사이드바 최상단 레일 아이콘, `NavFlyout.FlyoutLink` hover 별 토글)와 최근 방문(기기 로컬 `recentPathsStore`, 최대 5개) 진입 수단 신설로 잔여 해소. 기본 홈 탭(`home_prefs.defaultHomeTab`, Settings "화면 UI 설정"에서 선택)도 함께 추가해 로그인 시 선호 도메인부터 보여줄 수 있음 |

### R-4 6차 라운드 발견 (점검 체계 IA — 매트릭스 중심 재편 — 2026-09-08)

사용자 제보(riverjin839, 3라운드에 걸친 방향 피드백) 기반 점검 체계 감사. "Deep Check/Addon 방식의 의미
확인" 요청에서 출발해 "매트릭스가 클러스터 운영의 첫 화면이어야 한다", "점검 방식 용어(deep_check/addon)
대신 실제 실행 기술(k8s API/bash/python/ansible 등)과 컴포넌트 구분으로 명명하라", "항목마다 등록·수정·
테스트·운영적용·로그확인·히스토리 수집이 가능해야 한다"로 요구가 구체화됐다. `PlatformStatusMatrix.tsx`·
`CheckMatrixItemFormModal.tsx`·`CheckMatrixCellDetailModal.tsx`·`CheckMatrixRunbookPanel.tsx`·
`OpsCheckConsolePage.tsx`·`check_matrix_service.py`·`deep_checkers/*.py`·`checkers/*.py`·
`batch_job.py`·`playbook.py`·`homeStore.ts`·`navConfig.ts` 대조. 설계안(그림 5종 + 근거 11개)은
[점검 체계 통합 설계안](https://claude.ai/code/artifact/9d204e4a-c106-48e8-91b8-4d0b37ebc250) 참고.
**코드 변경 없음 — 등재만.**

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-061 | 첫 화면 가시성 | **점검 매트릭스가 홈의 탭 하나에 숨어 있고 기본 탭이 "업무 현황"** — 네비게이션 메뉴에도 매트릭스 진입 경로가 없다(`NAV_MAP` 미포함). 로그인 직후 클러스터 운영 상태를 한눈에 보려면 탭 전환이 필요 | 클러스터 운영 관리가 주 업무인 사용자가 매번 탭을 눌러야 하고, 메뉴 검색으로는 매트릭스를 찾을 수 없음 | 높음 | 완료 | 로드맵 1단계: 홈 기본 탭 `work`→`platform` 전환 완료(`homeStore.ts` `loadHomeTab()`). 네비게이션 진입 경로 추가는 후속 단계 |
| D-062 | 정보구조/명명 | **"자동 점검 (Deep Check)/(Addon)/수동 입력"이라는 구현 세부가 그대로 사용자 언어** — 실제로는 실행 기술(K8s API/kubectl/HTTP/PromQL/스냅샷/SSH bash·python/Ansible)이 섞여 있고 화면마다 배지 문구가 다르다(매트릭스 "핵심/Deep/Addon/수동" vs 콘솔 "점검/애드온/SSH/Ansible"). 같은 대상(etcd 4소스·4기술, 노드 5중, 인증서 임계값 상이)을 여러 소스가 중복 점검 | 운영자가 무엇을 등록하는지 알기 어렵고, 같은 컴포넌트를 어디서 몇 번 점검하는지 파악 불가 | 높음 | 부분 | 로드맵 1단계: 매트릭스 행의 "핵심/Deep/Addon/수동" 배지를 `ExecTechBadge` 로 교체 완료. 로드맵 2단계: 항목 등록을 실행기술 우선 마법사로 재구성(`GET /check-matrix/exec-techs` 카탈로그 — deep_check/addon/manual 하드코딩 목록 제거), 수정 폼의 라디오 문구도 "Deep Check/Addon" 문자열 제거. 잔여: 콘솔(`/ops-checks`)·`DailyCheckReview` 배지 통일, 대상 중복 표시(항목 상세=4단계) |
| D-063 | 드릴다운 부재 | **매트릭스에 클러스터명·항목명 클릭 이동이 없다** — `PlatformStatusMatrix.tsx` 전체에서 화면 이동은 클러스터 미등록 안내 1건뿐, `<Link>` 0건. 상세 설정은 텍스트로 "운영 점검 화면에서…"라고 안내만 함 | 기본 UI(매트릭스)에서 상세로 내려가는 경로가 없어 사용자가 다른 메뉴를 뒤져서 찾아가야 함 | 높음 | 부분 | 로드맵 1단계: 클러스터명 클릭 드릴다운 추가. 로드맵 3단계: 신규 `/clusters/:id`(종합 상태+원인 카드, `ClusterOpsCheckPanel` 공유) 완료, 클러스터명 링크를 `/ops-checks/:clusterId`→`/clusters/:id` 로 전환. 항목명 드릴다운은 `/checks/:itemId` 신설 시점(4단계)으로 보류 |
| D-064 | 라이프사이클 공백 | **저장 전 테스트(미리 실행) 수단이 매트릭스에 없다** — `POST /deep-check/definitions/preview`·`/{id}/test` API 는 있으나 `DeepCheckDefinitionForm`의 "미리 실행" 버튼만 쓴다. 매트릭스 항목 등록은 저장 버튼뿐이라 돌지 않는 항목이 그대로 저장될 수 있음 | "등록→테스트→운영적용" 흐름이 끊겨 있어 검증 없이 점검이 배포됨 | 높음 | 완료 | 로드맵 2단계: `POST /check-matrix/items/preview`(deep_check 는 `run_check_type_once(persist=False)`, addon 은 `HealthChecker.preview_addon_check()` — transient Addon 으로 저장 없이 실행) 신설, 등록 마법사에 "테스트" 단계로 통합(클러스터가 있으면 최소 1회 실행해야 다음 단계로 진행). 커스텀 deep_check(`seed_default=False`) 는 등록과 동시에 `ensure_deep_check_definition()` 이 글로벌 정의를 자동 생성해 즉시 실행 가능해진다 |
| D-065 | 라이프사이클 분산 | **cron(운영 적용)이 셀 상세·클러스터 배지·정의 화면 세 곳에 분산**되고, 임계값 편집 시 클러스터 전용 정의를 자동 생성하지 않아 글로벌 정의를 직접 수정한다(`docs/CHECK_MATRIX_GUIDE.md` §6.1 표가 이 분산을 문서로도 인정) | 클러스터별로 다르게 두려는 설정이 실수로 전 클러스터에 적용될 위험, 적용 지점을 못 찾아 헤맴 | 높음 | 부분 | 로드맵 2단계: `update_source_config()` copy-on-write 완료 — 해석된 정의가 글로벌이면 저장 직전 클러스터 전용 사본을 자동 생성해 그 사본만 수정(글로벌·다른 클러스터 무영향), `CheckMatrixRunbookPanel` 안내 문구도 "전 클러스터 적용 경고"→"자동 복제 안내"로 갱신. cron 3곳 분산(셀 상세·클러스터 배지·정의 화면) 통합은 후속 단계 |
| D-066 | 카탈로그 범위 | **배치잡(SSH bash/python)·플레이북(Ansible)이 매트릭스·운영 점검 콘솔 어디에도 실제 항목으로 없다** — 라벨(`SOURCE_LABEL`)만 정의돼 있고 카탈로그·실행 어댑터는 미구현("단계 1 이후"로 주석에 명시, `ops_check_service.py:8-9,70-74`) | "실제 기술요소에 맞게 등록·운영적용"을 요구했지만 SSH/Ansible 계열 점검은 여전히 별도 화면(BatchJobsPage)에 고립 | 높음 | 대기 | 제안: `CheckMatrixSourceType`에 `batch_job`/`playbook` 추가, `PlaybookRun` 이력 테이블 신설(플레이북은 현재 이력 테이블 없음) |
| D-067 | 상태 정합성 | **`Cluster.status` writer 가 2곳(DailyChecker·HealthChecker)이라 서로 덮어쓰고, 심층 점검은 전혀 반영되지 않는다.** 원인을 보여주는 화면도 없음 | 클러스터 배지가 왜 warning/critical 인지 알 수 없고, 조치가 먹혔는지 확인할 방법이 없음 | 높음 | 완료 | 로드맵 3단계: `services/cluster_status_service.py::recompute()` 단일 롤업 — `DailyChecker`/`HealthChecker`/`DeepCheckService` 세 곳 모두 자기 도메인 결과를 flush 한 뒤 이 함수만 호출(직접 대입 금지), addon 단일 실행(`run_single_addon_check`)의 reachability 무시 비대칭도 함께 해소. 심층 점검은 opt-in(`DeepCheckDefinition.affects_cluster_status`, 기본 false, 정의 폼에 토글). `GET /clusters/{id}/status-breakdown`(원인 목록) + `/clusters/:id` 페이지 + 매트릭스 `clusters[].status` + 대시보드 요약 1줄 |
| D-068 | 버그 | **애드온 등록 템플릿(`frontend/src/config/addons.config.ts`)의 `type` 값이 `core/networking/storage` 같은 카테고리 문자열이라 백엔드 `CHECKER_REGISTRY` 키(`etcd-leader` 등)와 전혀 매칭되지 않는다.** 이 템플릿으로 만든 애드온은 전부 조용히 Ansible 폴백 경로로 빠지고 매트릭스 컴포넌트 칩도 비게 됨 | 화면에서 고른 애드온 종류와 실제 실행되는 체크 로직이 다름 — 무엇을 점검하는지 착각하게 됨 | 높음 | 완료 | 조사 결과 이 파일은 실제 애드온 등록 화면(`AddAddonModal.tsx`)에서 전혀 import 되지 않는 **죽은 코드**였다(실제 등록은 이미 `CHECKER_REGISTRY` 와 일치하는 자체 `ADDON_TEMPLATES` 사용) — 실사용 버그는 아니었지만 방치 시 누군가 실수로 연결할 수 있는 지뢰라 파일과 `AddonConfig` 타입을 완전히 삭제 |

### R-4 7차 라운드 발견 (Main UI 상용화 진단 — 앱 셸·홈·로그인 — 2026-09-10)

사용자 요청("Main UI 상용화를 위한 UX/UI 개선 요청 진단"). 대상은 로그인 직후 사용자가 매번 거치는
**앱 셸 전체** — `index.html`(로더) → `LoginPage.tsx` → `App.tsx`(AppShell) → `Sidebar.tsx`·`NavFlyout.tsx`·
`navConfig.ts` → `AppTopBar.tsx` → `HomePage.tsx`(KPI 스트립·세그먼트 탭·`PlatformStatusMatrix`) → `services/api.ts`(세션).
기준: 사내 도구가 아니라 **다른 팀/고객에 배포되는 제품**이라고 가정했을 때 첫인상·탐색·권한·세션·국제화·접근성이
갖춰졌는가. **코드 변경 없음 — 등재만.** 양호 판정(재작업 불필요): 홈 EmptyState 에 실제 CTA 버튼(클러스터 등록/항목
추가) 제공, 세그먼트 탭 `role=tablist`+방향키, skip link, 레일 아이콘 전부 `aria-label`, `prefers-reduced-motion`
처리, 401 인터셉터 존재, 즐겨찾기/최근 방문/Your Island 개인화, "실행" 이벤트마다 수행 로그 패널(riverjin839 custom 규칙 준수).

| ID | 영역 | 문제 | 사용자 영향 | 심각도 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| D-069 | 브랜드/첫인상 | **구 제품명 "DEVOPS MANAGEMENT" 가 첫 두 화면에 남아 있다** — `index.html` 로더 "Loading DEVOPS MANAGEMENT...", `LoginPage.tsx:44` 카드 제목 `DEVOPS MANAGEMENT`. `<title>`·사이드바·`PRODUCT.md` 는 PEP. 로그인 카드에 제품 설명·버전·지원 링크도 없다 | 제품을 처음 보는 사용자가 두 개의 이름을 본다 — 리브랜딩(2026-05) 이전 흔적이 상용 첫인상에 그대로 노출 | 높음 | 완료 | 로더·로그인 제목을 `PEP` + 풀네임으로 통일, 로그인 카드에 버전(`package.json`)·"도움말" 링크 자리 확보 → **처리(2026-09-10)**: `index.html` 로더 문구·`LoginPage` 제목을 PEP/풀네임으로 통일, 카드 하단 `v{__APP_VERSION__}`(vite `define`←`package.json`). 도움말 링크는 D-077 셸 메뉴 정리와 함께 |
| D-070 | 초기 로드 | **매 로드마다 다크→라이트 테마 플래시(FOUC)** — `index.html` 이 `<html class="dark">` + 로더 배경 `#0a0a0a`/텍스트 `#fafafa` 하드코딩인데 기본 테마는 `default`(라이트 Claude paper). `themeStore` 가 모듈 로드 시점에야 클래스를 교체하므로 JS 파싱 전까지 검은 화면 → 흰 화면으로 튄다 | 라이트 테마 사용자(기본값)는 새로고침·첫 진입마다 화면이 번쩍인다. 상용 제품에서 가장 먼저 지적되는 품질 신호 | 높음 | 완료 | `index.html` `<head>` 에 `localStorage['k8s:theme']` 를 읽어 클래스를 미리 붙이는 3줄 인라인 스크립트 + 로더 색을 `--background` 토큰 값으로 → **처리(2026-09-10)**: `index.html` `<head>` 인라인 스크립트가 `k8s:theme` 를 읽어 `themeStore.applyTheme` 과 같은 규칙으로 `<html>` 클래스를 선적용, `class="dark"` 하드코딩 제거, 로더 색을 default/light/dark/electropop 토큰값으로 |
| D-071 | 테마 정합 | **주 네비게이션 flyout·툴팁이 10개 테마를 전부 무시하는 고정 라이트 팔레트** — `NavFlyout.tsx:45-55` `bg-white text-black border-zinc-200 bg-zinc-50 text-zinc-700`, `FLYOUT_LINK_INACTIVE='text-black hover:bg-zinc-100'`, `Sidebar.tsx:159` 툴팁 `bg-zinc-700 text-white`, 클러스터 flyout 섹션 헤더 `text-zinc-500 border-zinc-200`(370-371·642-643). CLAUDE.md 불변 규칙("고정 팔레트 금지") 위반이 셸 핵심부에 있다. 1회차 정량 grep 이 `gray-*` 만 검사해 `zinc-*`(전체 57건) 을 놓쳤다 | dark/electropop 테마에서 흰 팝오버가 튀고, 테마별 sidebar 토큰(`--sidebar-*`)을 flyout 만 안 따른다 | 높음 | 완료 | `bg-popover text-popover-foreground border-border` 로 치환, 툴팁은 `bg-foreground text-background`. 절차 A grep 패턴에 `zinc-` 추가(이번 감사에서 반영) → **처리(2026-09-10)**: `NavFlyout`(shell·헤더·닫기·링크·핀 별)·`Sidebar` 툴팁(`bg-foreground text-background`)·섹션 구분선/라벨·`FavoritesFlyoutBody` 전부 토큰화 — `components/layout/` 의 `zinc-*`/`amber-*` 0건 |
| D-072 | 테마 선택 UX | **테마 11종을 레일 버튼 1개의 순환 클릭으로만 고른다** (`THEME_CYCLE`, `Sidebar.tsx:519`) — 미리보기 없음, 현재 테마명은 툴팁으로만, 원하는 테마까지 최대 10클릭. 노벨티 테마 8종(tropical-punch·electropop 등)이 기본 3종(default/light/dark)과 동급으로 노출 | 테마를 바꾸려다 화면이 10번 바뀐다. 상용 제품에서 브랜드 톤을 흐리는 노벨티 테마가 기본 셸에 상시 노출 | 중간 | 대기 | 레일 버튼 → 스와치 그리드 팝오버(현재 표시·1클릭 선택). 기본 팝오버엔 default/light/dark/system 4종, 나머지는 Settings "테마 갤러리" 로 이동 |
| D-073 | 탐색 | **전역 검색/커맨드 팔레트 부재** — `NAV_MAP` 60 라우트·pages 70개인데 진입 수단은 hover flyout(레일 8그룹 + 상단바 2그룹)·즐겨찾기·최근 방문뿐. `Ctrl/⌘+K` 바인딩 0건, 화면명 검색 UI 는 Your Island 의 `PanelPickerDialog` 안에만 있다 | 화면 이름은 아는데 그룹을 모르는 사용자(신규 팀원·타 팀)는 8개 flyout 을 순서대로 열어본다. Grafana/Linear/Notion 급 제품의 기본 기대치 미충족 | 높음 | 대기 | `cmdk` 기반 팔레트: `useNavCatalog()` 의 라벨/`featureAllowed` 재사용, 화면 이동 + 클러스터 이름 점프 + 최근 방문. 상단바에 검색 입력 자리 |
| D-074 | 국제화 | **라벨 언어 혼용 + i18n 레이어 부재** — `NAV_MAP` 60개 중 22개가 영문(`Batch Jobs`·`Playbooks`·`Settings`·`Work To Do`·`Observability`·`Your Island`·`홈 (Today)`), `K8S 상세 관리`/`K8s 실시간 이벤트` 표기 혼재. `react-i18next` 등 번역 레이어가 없어 문자열이 컴포넌트에 직접 박혀 있다(`lang="ko"` 고정) | 한국어 화면에 영문 메뉴가 3분의 1 섞여 톤이 흔들리고, 영문 UI 를 요구하는 고객/팀에는 대응 자체가 불가능 | 높음 | 대기 | 1단계: NAV_MAP 라벨 한국어 통일 + `K8s` 표기 통일(`NavMenuManager` 오버라이드가 있으니 기본값만). 2단계: 셸(사이드바·상단바·홈·로그인)부터 i18n 키 추출 — 전면 i18n 은 로드맵 R-6 |
| D-075 | 탐색/식별 | **`document.title` 이 전 라우트에서 동일** — k9s/SSH 팝업 2곳만 갱신, 나머지 68페이지는 "PEP — Platform Engineering Portal" | 브라우저 탭·히스토리·북마크에서 어느 화면인지 구분 불가. 운영자는 탭을 6~10개 띄워 놓고 쓴다 | 중간 | 완료 | `usePageTitle()` 훅 1개를 `PageStyleProvider` 에서 `NAV_MAP`/`getLabel` 로 자동 설정("화면명 · 클러스터명 · PEP") → **처리(2026-09-10)**: 신규 `hooks/usePageTitle.ts` 를 `PageStyleProvider` 에서 호출 — NAV_MAP 최장 접두 일치 + `getLabel` + Settings `appTitle`. 미등록 경로는 "앱 제목 — Platform Engineering Portal". 클러스터명 병기는 후속 |
| D-076 | 반응형 | **최소 폭·태블릿 대응 없음** — `Sidebar` 고정 56px 에 breakpoint 0, `HomePage` 그리드는 `xl:` 에서만 2열, `AppTopBar` 업무 nav 는 `overflow-x-auto` 로 잘려 스크롤바 없이 가려지고, KPI 필 6개는 `flex-wrap` 으로 2~3줄로 접힌다. 최소 폭 가드는 `SidePane minWidth=420` 뿐 | 13" 노트북(1280px)·태블릿에서 상단바 항목이 보이지 않게 잘리고 홈 위 폴드가 KPI 줄에 잠식된다. 상용화 시 검증 매트릭스가 없다 | 중간 | 대기 | 지원 뷰포트 선언(예: ≥1280 정식, 1024~1279 축약, <1024 경고 배너) + 상단바 항목 `lg:` 미만에서 "더보기" 드롭다운으로 접기 |
| D-077 | 셸 IA | **레일 하단 개인 존 과밀 + 개발 내부 산출물 상시 노출** — 설정/테마/아일랜드/AI/사용자/VOC/로그아웃 7개 무라벨 아이콘. "사용자 VOC 게시판 · 릴리즈 노트 · 버그 픽스 로그"(`Sidebar.tsx:558`) 는 제품 셸에 개발팀 내부 산출물(버그 픽스 로그)을 노출한다. 로그아웃이 확인 없이 1클릭이며 VOC 아이콘과 인접. 도움말 진입점은 매트릭스 안 `CheckMatrixHelpPanel` 뿐 | 상용 사용자가 "버그 픽스 로그" 를 볼 이유가 없고, 로그아웃 오클릭으로 작성 중인 폼을 잃는다. 제품 수준 도움말/지원 메뉴가 없다 | 중간 | 대기 | 하단 존을 **사용자 메뉴(아바타)** 하나로 접고 그 안에 테마·비밀번호·로그아웃(확인). VOC/릴리즈 노트/버그 로그는 "도움말·지원(?)" 메뉴로 격리, 버그 픽스 로그는 admin 전용 |
| D-078 | 홈 어포던스 | **KPI 필 6개가 시각은 같은데 동작이 3종** — Your Island(목적지)·4개 필(라우트 이동 `Link`)·"점검 실패"(라우트 이동 없이 탭 전환 `button`, `HomePage.tsx:242`). "다음 일정" 은 해당 항목이 아니라 `/tasks-mgmt` 목록으로 간다 | 같은 모양을 눌렀는데 어떤 건 페이지가 바뀌고 어떤 건 아래 탭만 바뀐다 — 학습 비용. "다음 일정" 클릭 후 목록에서 다시 찾아야 한다 | 낮음 | 대기 | 탭 전환형 필에 화살표/언더라인 등 구분 표식 또는 전부 라우트로 통일(`/?tab=platform`). "다음 일정" 은 `/tasks-mgmt/:id` 로 |
| D-079 | 세션 | **세션 만료가 무언 로그아웃** — `api.ts:135` 401 → `clearAuthSession()` 즉시, "세션이 만료되었습니다" 안내·복귀 경로(`returnTo`)·미저장 입력 보호 없음. 토큰 24h 고정(`access_token_expire_minutes`), refresh 없음. 로그인 폼에 SSO(OIDC/LDAP) 진입이 없다(`auth.py` 에 관련 코드 0건) | 긴 폼(업무 등록·점검 항목 마법사) 작성 중 만료되면 입력이 통째로 사라지고 로그인 후 홈으로 떨어진다. 기업 고객은 SSO 없이는 도입 심사를 통과하지 못한다 | 높음 | 대기 | 프론트: 만료 토스트 + `returnTo` 쿼리 + 임박 경고(만료 5분 전). 백엔드: refresh 토큰 또는 sliding 만료, OIDC 는 로드맵 R-7(백엔드 과제라 UX 백로그에서는 진입점 자리만) |
| D-080 | 접근성 | **flyout 이 키보드로 진입 불가** — `NavFlyout.tsx` 가 `role="dialog"` 인데 열릴 때 포커스 이동·트랩이 없고(`focus()` 호출 0건, `useModalA11y` 미사용) portal 이 `document.body` 끝에 붙어 레일 버튼에서 `Tab` 을 눌러도 flyout 링크에 도달하지 못한다. hover 로만 열리는 동작은 터치에서도 불리 | 키보드·스크린리더 사용자는 하위 메뉴(플랫폼 도메인 화면 40여 개)에 아예 들어갈 수 없다 — WCAG 2.1.1 위반. 상용화 접근성 심사 탈락 항목 | 높음 | 대기 | `role="menu"` + 열릴 때 첫 항목 포커스, ↑↓/Home/End/Esc, 닫힐 때 트리거로 복귀. `useModalA11y` 재사용 |
| D-081 | 정리 | **레거시 `Header.tsx` 잔존** — `components/layout/index.ts` 가 export 하지만 사용처 0. `text-white`·`rgba()` 그림자·구 nav('Dashboard'→`/`) 등 현행 규칙 위반 코드가 살아 있어 신규 작업 시 잘못된 레퍼런스가 된다 | 정량 감사 오염 + 실수로 재사용 위험 | 낮음 | 완료 | 파일·barrel export 삭제 → **처리(2026-09-10)**: `components/layout/Header.tsx` + barrel export 삭제 |
| D-082 | 권한 UX | **viewer 에게도 실행·추가·설정 버튼이 동일하게 보인다** — `components/platform-status/*` 에 role 분기 0건(도움말 텍스트에만 "operator 이상"). 백엔드 `check_matrix.py` 는 `require_operator` 로 막으므로 클릭 후 403 토스트로 실패한다 | 읽기 전용 사용자가 "실행" 을 누르고 나서야 권한이 없음을 안다. 상용 RBAC 는 "보이지만 눌리지 않고 이유가 보이는" 상태가 기본 | 중간 | 대기 | `useAuthStore` role 로 실행/추가/설정 버튼 `disabled` + `title="operator 이상 권한 필요"`, 공용 `useCanOperate()` 훅 |
| D-083 | 버그 | **배치잡 "즉시 실행"이 `enabled=false` 여도 그대로 실행됐다** — 스케줄 디스패처(`run_batch_job_dispatcher`)와 일괄 실행(`bulk_run_jobs`)은 `BatchJob.enabled` 를 걸렀지만, 단일 잡 실행 엔드포인트(`POST /batch-jobs/{id}/run`, 테이블 ▶ 아이콘/슬라이드오버 "실행" 폼이 호출)만 이 검사가 빠져 있었다 | 사용자가 배치잡을 껐다고 믿고도 클릭 한 번으로 실제 클러스터에 명령이 나갈 수 있었음(사용자 제보: "batchjob 수행을 껐는데 k8s cluster job 이 실행되는 것 같다") | 높음 | 완료 | `run_job()` 에 `enabled` 가드 추가(400) + 프론트 두 진입점(`BatchJobRow`/`BatchJobSlideOver.RunForm`) 동일 가드·비활성 배지·사유 툴팁. `k8s_job_cleanup` 은 애초에 영속 K8s Job/CronJob 리소스를 만들지 않고 `kubectl get/delete jobs` 만 수행하므로(조사로 확인) "꺼도 남는 리소스" 정리는 불필요 — "꺼짐"의 의미를 "앞으로의 새 실행(스케줄+수동)만 차단"으로 명확히 확정 |
| D-084 | 정보 과밀 | **점검 매트릭스가 항목 수만큼 그대로 세로로 늘어나 admin 이 "지금 어느 영역이 문제인가"를 스크롤 없이 파악할 수 없었다** — 행 그룹핑·요약 배지가 없어 항목이 늘수록 첫 화면 유용성이 선형으로 떨어짐 | 점검 항목이 늘어날수록 핵심 운영 포인트(K8s 등)를 찾기 위한 스크롤/탐색 비용이 커짐 | 중간 | 완료 | 행을 `item.category` 기준 그룹(K8s/네트워크/스토리지/OS/애플리케이션 순)으로 묶고 그룹 헤더에 정상/경고/위험 카운트 배지 상시 노출, 기본은 접되 위험/경고가 있는 그룹만 자동 펼침(이후 사용자 선택은 `localStorage` 로 유지). 기존 "화면당 표시 행 수" 페이지네이션은 그룹 접기로 대체 |
| D-085 | 버그 | **셀 상세 "실행 방식" 탭의 실행 단계 타임라인이 실행 중엔 항상 회색(대기)으로만 보였다** — `steps={running ? undefined : latestRun?.steps}` 가 실행 중일 때 실제 진행 상태를 의도적으로 지워, "재생" 버튼을 눌러도 색이 바뀌지 않음(재생은 재조회가 아니라 로컬 reveal 애니메이션 리셋일 뿐) | 실행 중인 점검이 지금 어느 단계까지 왔는지 실시간으로 확인할 수 없어 완료까지 무작정 기다려야 했음 | 중간 | 완료 | 백엔드가 실행 중 단계도 이미 `running` 상태로 채워 2초 폴링에 실어 보내고 있었으므로 그 값을 그대로 넘기도록 수정 — 완료를 기다리지 않아도 단계별 진행이 실시간으로 색칠됨. 같은 탭의 레이아웃도 대상/최근수행 1행 + 명령/설정 2열로 재배치하고 각 목록에 내부 스크롤 캡(`max-h-72`)을 둬 스크롤 없이 한눈에 보이는 범위를 넓힘(탭 전용 모달 폭 `max-w-4xl`) |

**상용화 우선순위 제안** (구현 난도 대비 첫인상 효과 순):

| 단계 | 목표 | 항목 | 규모 |
|---|---|---|---|
| P0 첫인상·정합 | 이름 하나, 깜빡임 없음, 테마 일관 | D-069 · D-070 · D-071 · D-081 · D-075 | 1~2일 |
| P1 탐색·권한·세션 | 어디든 3초 안에, 안 되는 건 이유가 보이게 | D-073 · D-080 · D-082 · D-079(프론트 부분) · D-077 | 1~2주 |
| P2 국제화·반응형·테마 정리 | 다른 언어·다른 화면 크기에서도 같은 제품 | D-074 · D-076 · D-072 · D-078 | 로드맵 R-6/R-7 과 연동 |

---

## 3. 고도화 로드맵 (Roadmap)

| 테마 | 내용 | 선행 조건 | 상태 |
|---|---|---|---|
| R-1 토큰 정합 완결 | DESIGN_SYSTEM.md W1 마무리 — raw hex 전수 치환(화이트리스트 외 0건) | 백로그의 hex 관련 항목 처리 | 진행 중 |
| R-2 shadcn/ui 확산 | W2 — Button/Card/Badge/Tooltip/Dialog 5종 우선 도입, 기존 자체 컴포넌트 어댑터화 | R-1 | 진행 중 |
| R-3 접근성 상시화 | W4 성과(jsx-a11y 룰 상시화) 위에 sr-only 데이터 표·Lighthouse 정량 측정 추가 | - | 대기 |
| R-4 화면 단위 UX 리뷰 | docs/SCREENS.md 기준 주요 화면 순회 정성 리뷰 | 감사 1회차 완료 ✅ | 진행중 — 1차(홈·대시보드·운영점검·업무, D-011~D-019) 처리 완료(D-018 모달→Dialog 리팩터만 보류), 2차(K8s상세/자원관리·일일점검·LAKE, D-020~D-028) 완료. 3차(클러스터 정보 수정, D-031~D-040) 완료. 4차(클러스터 관리 목록 `/cluster-manage`, D-041~D-053) 전 항목 완료 — 화면 백로그 소진(기능 버그 8건 + 토큰/MacCard/레이아웃/a11y/잔여 5건). 5차(홈 모드·전역 IA, D-054~D-060) 전 항목 완료 — 화면 백로그 소진. **6차(점검 체계 IA — 매트릭스 중심 재편, D-061~D-068) 등재 — 로드맵 3단계까지 진행(D-061·D-064·D-067·D-068 완료, D-062·D-063·D-065 부분), 4~5단계 대기.** 다음 라운드 후보: 인프라 토폴로지·서비스 토폴로지·Pod 병목 |
| R-6 국제화(i18n) | 셸(사이드바·상단바·홈·로그인)부터 문자열 키 추출 → react-i18next 도입, ko/en 2개 로케일. NAV_MAP 라벨 통일(D-074) 이 1단계 | D-074 1단계 | 대기 |
| R-7 상용 세션/SSO | 만료 안내·returnTo·refresh 토큰(프론트+백엔드), OIDC/LDAP 로그인 진입점(D-079). 백엔드 과제라 DESIGN.md 는 UX 진입점만 추적 | D-079 프론트 부분 | 대기 |

---

## 4. 점검 이력 (Audit Log)

| 일자 | 범위 | 신규 | 해결 | 수행 | 비고 |
|---|---|---|---|---|---|
| 2026-07-19 | 체계 구축 + 1회차 전수 감사 (`frontend/src` grep 정량 + 구조 점검) | 8 (D-001~D-008) | 0 | ux-ui-designer | ClusterSidebar iconOnly·클러스터 select 금지 완전 준수 확인. 우선순위: D-002 → D-003 → D-004 → D-005 |
| 2026-07-19 | 백로그 전량 처리 (D-001~D-008) | 0 | 8 | ux-ui-designer | 문서 현행화(D-001/002)·차트 토큰 신설(D-005)·MacCard 수렴 1차(D-004)·aria-label 33건(D-007) 등. D-003 은 감사 오탐 정정 후 잔존 5건 처리. lint/tsc/build 전체 게이트 통과. PR #478 (머지, v1.7.0) |
| 2026-07-20 | 미진행분 2차 처리 (D-009·D-010) | 2 | 2 | ux-ui-designer | MacCard 2차 수렴 9페이지 28건 + pages aria-label 83건. 보류: 보드/캔버스형 페이지 MacCard(R-4 연계), 행 네비게이션 키보드 접근(R-3 연계). lint/tsc/build 게이트 통과 |
| 2026-07-20 | R-4 1차 라운드 — 화면 정성 리뷰 4화면 + 처리 (D-011~D-019) | 9 | 5.5 | ux-ui-designer | 홈·대시보드·운영점검·업무 리뷰로 32건 발견→9항목 등재. **완료** D-011(상태색 토큰 4화면)·D-012(에러상태)·D-013(편집셀 a11y)·D-016(MacCard/레이아웃)·D-019(KST·삭제문구). **부분** D-015·D-017. **보류** D-014(위험실행 확인·사용자 판단 필요)·D-018(모달 리팩터). lint/tsc/build 통과. 병렬 에이전트가 공유 워킹트리에서 `git stash` 로 충돌 → stash 복구로 무손실 수습(교훈: 다중 에이전트 파일수정은 worktree 격리 필요) |
| 2026-07-20 | D-014 처리 (사용자 결정: 모든 실행 = 운영 위험) | 0 | 1 | ux-ui-designer | 운영 점검 개별·선택 실행에 ConfirmDialog(danger, 대상·건수·소스 요약) 게이팅. lint/tsc/build 통과 |
| 2026-07-20 | R-4 2차 라운드 — 화면 정성 리뷰 4화면 + 처리 (D-020~D-028) | 9 | 6 | ux-ui-designer | K8s상세관리·K8s자원관리·일일점검·LAKE 리뷰로 발견 9항목 등재. **완료** D-020(상태색 토큰 4화면)·D-021(에러상태)·D-022(위험동작 ConfirmDialog)·D-024(색 단독). **부분** D-023·D-025·D-028. **보류** D-026(모달→Dialog)·D-027(skeleton). git 금지 조건으로 파일 소유 분리 병렬 처리 → 1차 stash 충돌 재발 없음. lint/tsc/build 통과 |
| 2026-07-20 | 잔여 중요항목 처리 — D-026 모달 접근성 | 0 | 0.5 | ux-ui-designer | 재사용 훅 `useModalA11y`(Escape·포커스 트랩·초점 복원) 신설 후 앱 전역 `ConfirmDialog`+운영점검 상세·일일점검 Schedule/Items 모달에 적용(role/aria-modal/aria-labelledby). PR #491 머지로 브랜치를 최신 main(v1.8.1) 재시작 후 재적용 — main 이 독립적으로 진행한 ConfirmDialog 토큰화와 충돌 1건(danger 색)은 main 선택(status-critical)으로 통일. 나머지 raw 모달 22곳은 점진 확산. lint/tsc/build 통과 |
| 2026-07-21 | 사용자 제보 — 전역 뒤로가기 부재 (D-029) | 1 | 0.5 | ux-ui-designer | 진단: 모달 히스토리 미연동(popstate 0건)·back 버튼 36개 중 navigate(-1) 2개·필터 URL 저장 10/63페이지. 1차 처리: 사이드바에 전역 뒤로가기 버튼 추가(history.state.idx 기반, 홈 fallback). 모달-히스토리 연동은 후속. lint/tsc/build 통과 |
| 2026-07-24 | 사용자 제보 — Settings 서비스 정보구조 (D-030) | 1 | 1 | ux-ui-designer | ①탭 개칭(LAKE 타입→관리 서비스) ②PEP/APP 도메인 재편(PEP=인프라 10종·APP=사용자서비스 4카테고리, 멱등 마이그레이션) — PR #525 머지. ③최상위 "서비스" 탭을 "관리 서비스" 내부 서브탭(서비스 타입/서비스 카탈로그)으로 이동. backend 테스트 순서의존 버그(pgvector)도 함께 수정. 전 게이트 통과 |
| 2026-07-24 | R-4 잔여 부분완료 항목 마무리 (D-015·D-023·D-025·D-028) | 0 | 4 | ux-ui-designer | 접근성/반응형 잔여분 완결: CheckHistoryHeatmap 상시 범례(색+라벨) 추가(D-015), K8sManage DetailDrawer 에 useModalA11y(Escape·포커스트랩·role=dialog) 적용(D-023), K8sAllocation 노드 카드 그리드 `minmax(min(220px,100%),1fr)`+가로 스크롤(D-025), K8sAllocation hover 툴팁(StatTooltip/PodScheduleCalc)을 button+group-focus-within 으로 키보드/터치 접근화(D-028). 네 항목 모두 완료 처리. lint/tsc/build 통과 |
| 2026-07-24 | R-4 잔여 로딩/반응형 항목 마무리 (D-017·D-019) | 0 | 2 | ux-ui-designer | 로딩 skeleton 구조화 — PlatformStatusMatrix 텍스트→매트릭스 구조 skeleton, WorkItemBoard 칸반→실제 5컬럼(헤더+카드) skeleton, HealthHero skeleton rounded-2xl→rounded-md(D-017). 반응형/a11y — HomePage work 모드 xl 미만 이중 스크롤 해소(그리드 높이 채움 xl 한정+패널 min-h), OpsCheckConsole 테이블 overflow-x-auto 래핑, WIP ⚠ 이모지→AlertTriangle(aria-label) 2곳+Kanban WIP 배지 고정팔레트→status 토큰(D-019). 두 항목 완료 처리. lint/tsc/build 통과 |
| 2026-07-24 | K8s상세관리·일일점검 로딩 skeleton (D-027) | 0 | 1 | ux-ui-designer | K8sManage 에 공용 Skeleton 기반 `RowsSkeleton`(헤더 그리드 아래 행 자리표시) 신설 후 리소스/Nodes/Pods/Helm/CRD(목록·오브젝트) 패널 로더 교체, 개요 패널은 3-스탯 카드 skeleton, DailyCheckReview AI 리뷰 로더는 텍스트 라인 skeleton. 텍스트 "불러오는 중…"→구조 skeleton 으로 레이아웃 시프트 감소. 항목 완료 처리. lint/tsc/build 통과 |
| 2026-07-24 | 모달 접근성 확산 1차 (D-026) | 0 | 0.3 | ux-ui-designer | 폼/다이얼로그형 전용 모달 8종(AddLakeServiceModal·AddServiceInstanceModal·ProjectFormModal·ClusterUpdateDiffDialog·ImageDistributeDialog·PlaybookLogDialog·CreateBatchJobWizard·BatchJobSlideOver overlay)에 useModalA11y 적용 + role/aria-modal/aria-labelledby 부여, 수동 Escape effect 통합 제거. useModalA11y 사용 파일 38→46. 남은 fixed inset-0 다수는 드롭다운/팝오버/터미널로 모달 아님. 잔여 페이지형 모달·SidePane 은 다음 라운드. lint/tsc/build 통과 |
| 2026-07-25 | 클러스터 정보 수정 — 상태·동작 마무리 (D-037·D-038·D-039) → **화면 백로그 소진** | 0 | 3 | ux-ui-designer | 미저장 이탈 확인(ConfirmDialog + beforeunload)·저장 성공 toast, 탭 `?tab=` URL 영속화(`replace:true` 로 히스토리 미적재), `hydrated` boolean→`hydratedId` 로 클러스터 전환 시 재하이드레이션. D-031~D-040 전 항목 완료로 이 화면 백로그 소진. 한계 명시: 선언형 BrowserRouter 라 `useBlocker` 불가 → 앱 내 링크 이동은 미차단. lint/tsc/build 통과 |
| 2026-07-25 | 클러스터 정보 수정 — 디자인 시스템·접근성·문서 처리 (D-034·D-035·D-036·D-040) | 0 | 4 | ux-ui-designer | 표현·접근성 계열 일괄 처리. 고정 팔레트 4섹션→categorical `chart-1~4` 토큰(스캔 8건→0), 수제 카드→`MacCard`+입력 `rounded-xl`/그룹박스 `rounded-md`+shadcn `Button`(`rounded-lg` 잔여 0), 탭 WAI-ARIA(tablist/tab/tabpanel·roving tabindex·←/→·Home/End)+에러 `role="alert"`+`text-[10.5px]`→`text-xs`(임의 폰트 0). SCREENS.md 의 Coroot 오기재 3곳 제거 + 이번 라운드 화면 구조 현행화. lint/tsc/build 통과 |
| 2026-07-25 | 클러스터 정보 수정 — 기능 버그 3건 처리 (D-031·D-032·D-033) | 0 | 3 | ux-ui-designer | 3차 라운드 중 심각도 높음 3건을 한 배치로 수정(같은 파일·상호 연관). ①빈 입력 `undefined`→`null` 24필드(타입도 `\| null`) — 값 해제가 실제로 저장됨 ②로딩/조회실패/미발견 3분기 + skeleton — 입력 유실·저장 무반응 제거 ③`formatApiError` + 형식 검증 15필드(인라인 오류·오류 탭 자동 전환). 사전 검증: 인터셉터가 `null` 보존, 대상 컬럼 전부 nullable, 검증 규칙이 기존 `parseCidrRange` 와 동일 형식. lint/tsc/build 통과 |
| 2026-07-25 | R-4 3차 라운드 — 클러스터 정보 수정(`/cluster-manage/:id/edit`) 정성 리뷰 (D-031~D-040) | 10 | 0 | ux-ui-designer | 사용자 요청 점검. 화면 코드(433행) + 백엔드 `update_cluster`/`ClusterUpdate` 스키마 대조로 **확인된 기능 버그 3건(높음)**: ①빈 값 저장 불가(undefined 직렬화 × `exclude_unset=True`) ②로딩 미처리로 입력 유실·저장 무반응 ③실패 사유 은폐+검증 부재. 그 외 고정 팔레트 8곳(다크 전용 톤 포함)·MacCard/라운딩 이탈·탭 ARIA 전무·이탈 경고 없음·탭 URL 미저장·`hydrated` 클러스터 미반응(잠재)·SCREENS.md Coroot 기술 오류(코드 0건). 코드 변경 없음 — 등재만 |
| 2026-07-29 | R-4 4차 표현·접근성·잔여 처리 (D-049~D-053) → **화면 백로그 완전 소진** | 0 | 5 | claude-code | ①고정 팔레트 **115건→0건** — `STATUS_STYLE`→`status-*`, `OVERLAP_COLORS`/`IpGroupRow` ACCENT/bond·BGP·버전 배지→categorical `chart-*`, 미사용 `OPERATION_LEVELS`/`LEVEL_BADGE` 제거, 공용 `NodeNicsCollectModal` 포함. 라이트 전용 톤(`cyan-700`/`amber-700`)·다크 전용 톤(`*-400`) 혼재 해소 ②`MacCard` 0건→3곳(필터 패널·표 컨테이너·로딩 skeleton), `ClusterCard` `rounded-md`+`mac-shadow` ③`DoubleScrollX` 에 `bodyClassName` prop 신설 → 표 본문 `max-h`+`thead sticky`(inset shadow 로 구분선), 헤더 `flex-wrap`, 전 `td` `overflow-hidden`+이름/호스트명 `truncate`(리사이즈 안전), 카드 그리드 `min()` ④편집·드래그 진입점 `focus-visible` 노출, 아이콘 버튼 `aria-label` 9개(클러스터명 포함), 그룹헤더 이모지→`Globe`/`Tag`+텍스트 ⑤필터/뷰 `?q&level&sort&group&view` URL 영속화, CIDR 옥텟 검증+배지 문구 정정, 사문 inline width 제거, `EmptyState`+CTA 3종. lint/tsc/build·docs-sync 통과. 범위 밖 이월: `useOperationLevels.COLOR_BADGE`(전 화면 공용) |
| 2026-07-29 | R-4 4차 잔여 기능 버그 처리 (D-045·D-046·D-047) → **기능 버그 백로그 소진** | 0 | 3 | claude-code | ①정렬-드래그 정합 — 드래그를 수동 정렬에서만 활성, **테이블 뷰 행 드래그 신설**(DndContext+그룹별 SortableContext, 이름 셀 그립), seq fallback `?? 1000` 통일 ②이름 표준화 모달 초기화 effect 를 닫힘 시 리셋+열림 중 편집 보존 머지로 재구성(30초 폴링 리셋 제거) ③auto-update 를 per-cluster `Set`/`Map<AbortController>` 로 — 동시 수집·개별 중지, 열린 diff 다이얼로그를 다른 클러스터 응답이 덮지 않음(toast 안내). 행/카드 prop `autoUpdatingId`→`autoUpdating: boolean`. lint/tsc/build 통과 |
| 2026-07-29 | R-4 4차 기능 버그 일괄 처리 (D-041~D-044·D-048) | 0 | 5 | claude-code | 높음 5건 한 배치 수정. ①인라인 편집 빈 값 `null` 전송(5필드, 타입 `ClusterManageUpdate` 반영) ②저장 실패 토스트(호출부 처리 — Settings 인라인 에러와 이중 고지 방지)+커스텀 셀 try/catch·`beginEdit` draft 초기화 ③목록 로딩/실패/0건 3분기(skeleton·`formatApiError`+재시도) ④드래그 순서를 전체 clusters 기준 `arrayMove` 로 재구성(가려진 클러스터 자리 보존) ⑤삭제 `ConfirmDialog`(danger·연쇄 범위 명시)+일괄 수집 확인/진행률 n/N/재클릭 중단/결과 4분기 토스트. lint/tsc/build 통과(backend 무변경 — pytest 는 CI) |
| 2026-07-29 | R-4 5차 라운드 — 홈 모드 분기·전역 네비게이션 IA 감사 (D-054~D-060) | 7 | 0 | claude-code | 사용자 제보("모드 분기가 혼란") 기반. 클릭 수 실측: 동일 모드 내 2클릭 vs 교차 모드 4클릭, 지식 라우트 8개 도달 불가(고아). 원칙 평가: 3-click(교차 모드·지식 위반)·Hick's(클러스터 flyout 20항목)·정보 냄새(반대 모드 단서 0)·일관성(로고 3역할·레일 동작 2종). 개선안 3개 비교 후 B안(사이드바 통합+홈 탭) 권장. 코드 변경 없음 — 등재만 |
| 2026-07-29 | R-4 4차 라운드 — 클러스터 관리 목록(`/cluster-manage`) 레이아웃·버그 정성 리뷰 (D-041~D-053) | 13 | 0 | claude-code | 사용자 요청("레이아웃 점검 및 버그 audit"). 페이지(687행)+`components/cluster-manage/*`(11개) 를 백엔드 `reorder_clusters`/`update_cluster`·`useCluster(s)` 훅·api 인터셉터와 대조. **확인된 기능 버그 7건**: ①인라인 편집 값 해제 불가(D-031 동일 클래스 잔존) ②저장 실패 무고지+커스텀 셀 stale draft ③로딩/실패를 "클러스터 없음" 으로 위장 ④필터 중 드래그 시 부분 seq 재할당으로 순서 오염 ⑤정렬 모드와 드래그 불일치 ⑥이름 표준화 모달이 30초 폴링마다 입력 초기화 ⑦동시 auto-update 레이스. 그 외 삭제 native confirm·일괄수집 무확인/무취소, 고정 팔레트 115곳(다크·라이트 전용 톤 혼재), MacCard 0건, 헤더 no-wrap·thead 비고정·셀 클리핑 부재, 편집셀 키보드 미접근. 코드 변경 없음 — 등재만 |
| 2026-07-24 | 모달 접근성 확산 2차 — D-026 완료 | 0 | 0.7 | ux-ui-designer | 페이지형/공용 모달 12종(Sprints 2·Users 2·InfraTopology 폼+삭제확인·Settings 클러스터/관리서버·NodeSpec HostFacts·MindMap NodeEditor·LakeServiceTypeManager·ServiceCategoryManager·공용 SidePane)에 useModalA11y 적용. SidePane 은 disableEscape 존중 래핑. InfraTopology 삭제확인의 red 고정팔레트도 status 토큰화. useModalA11y 사용 파일 46→55, 남은 fixed inset-0 16곳은 전부 비모달(드롭다운/팝오버/터미널/Dialog 프리미티브) 확인 → D-026 완료. lint/tsc/build 통과 |
| 2026-08-05 | R-4 5차 라운드 처리 (D-054~D-057·D-060) + PEP/APP 서비스 완전 삭제 | 0 | 4.5 | claude-code | 사용자 요청("메인 UI 설계 개선"). **D-054**: `GROUPS.modes`(게이팅) → `GROUPS.domain`(배치만 결정)으로 전환, work 도메인(협업/문서 관리)을 신규 전역 `AppTopBar.tsx` 로 이동, `Sidebar.visibleGroups` 는 `domain==='platform'` 만 필터해 게이팅 완전 제거 — 두 도메인이 항상 동시에 보임. **D-055**: `handleHomeClick` 을 위치 무관 `navigate('/')` 단일 동작으로 단순화, 홈 화면 선택은 `HomePage.tsx` 의 라벨 있는 세그먼트 탭(`role=tablist`)으로 대체. **D-056**: `authStore.setSession()` 의 `setMode('work')` 강제 리셋 제거, `homeStore` 를 `mode`→`homeTab`/`pep:homeMode`→`pep:homeTab` 로 개명(구 키 1회 마이그레이션). D-057 은 `/k8s-logs` 편입만 부분 처리(잔여: `/jira-import`). **D-060 부분**: `useCheckMatrixFailureCount()`(신규, `useCheckMatrixGrid()` 와 쿼리키 공유) 로 "점검 실패" KPI 필 추가 + 플랫폼 탭 배지(위험 클러스터+점검 실패 합계, 0건 숨김) — 잔여는 즐겨찾기/최근 방문(4단계 예정). 부수 정리: PEP/APP 서비스(과거 사이드바 "PEP 서비스"/"APP 서비스" 그룹, `/services`·`/pep-services`·`/app-services`)가 문서 관리 그룹과 기능 중복이라 프론트+백엔드 완전 삭제(업무 관리 "관련 서비스" 태그 포함) — LAKE 서비스·Settings 관리 서비스는 별개 인프라라 무영향. 전역 상단바 도입에 따라 `index.css` 에 `--topbar-h` + `.app-min-h-screen`/`.app-h-screen`/`.app-max-h-screen` 유틸리티 신설, 60여 개 페이지의 `min-h-screen`/`h-screen` 을 기계적으로 치환(Login/ChangePassword/팝업 라우트는 제외). lint/tsc/build/pytest(신규 실패 0) 통과, Playwright 로 4테마·다크모드·홈 탭 전환·KPI 클릭·키보드 내비게이션 수동 검증 |
| 2026-08-05 | R-4 5차 라운드 D-060 잔여 처리 — 홈 개인화 (기본 탭·즐겨찾기·최근 방문) | 0 | 2 | claude-code | 사용자 요청("메인 UI 설계 개선" 4단계). **D-060 완료**: 신규 `GET/PUT /api/v1/me/home-prefs`(`home_prefs.py`, `user_settings` 재사용이라 스키마 변경 없음)로 기본 홈 탭 저장 — 로그인 후 최초 1회 서버값을 `homeStore` 에 반영, 탭 변경 시 500ms 디바운스 저장(Settings "화면 UI 설정" 탭에 선택기 추가). 즐겨찾기(`pinnedPaths`, `useFavorites`)는 `AppTopBar`★ 드롭다운과 사이드바 최상단 "즐겨찾기" 레일 아이콘 두 진입점이 공용 `FavoritesFlyoutBody` 를 공유, `NavFlyout.FlyoutLink` 에 hover 시 별 토글 버튼 추가. 최근 방문은 기기 로컬 `recentPathsStore`(최대 5개, `App.tsx` 의 `RouteAccessGate` 가 라우트 진입마다 기록). 부수 수정: 2단계에서 `Sidebar` 홈 아이콘이 이미 단일화됐는데 `SettingsPage` 가 여전히 업무/플랫폼 2행으로 남아있던 드리프트를 1행으로 정리. Playwright 로 즐겨찾기 토글(상단바+사이드바)·최근 방문 노출·기본 홈 탭 저장 후 재로그인 반영을 수동 검증(폰트 CDN 이 샌드박스에서 unreachable 이라 `networkidle`/스크린샷이 `document.fonts.ready` 대기로 멈추는 환경 문제를 발견해 Google Fonts 요청을 라우트 차단하고 `waitUntil:'load'` 로 우회). lint/tsc/build/pytest 통과 |
| 2026-08-05 | R-4 5차 라운드 잔여 D-057~D-059 처리 + 사이드바 flyout 호버 오픈 | 0 | 1.5 | claude-code | 사용자 요청("잔야 작업 확인" 후 "한번에 처리"). **D-057 완료**: 마지막 고아 라우트였던 `/jira-import` 를 `navConfig.ts` 의 `collab`(협업) 그룹에 편입 — 5차 라운드 D-054~D-060 전 항목 완료. **D-058 완료**: `Sidebar.renderFlyoutBody` 가 `cluster` 그룹(20여 항목)만 `CLUSTER_FLYOUT_SECTIONS`(모니터링/콘솔/점검/관리) 로 재배열해서 렌더 — `GROUPS.cluster.paths` 는 그대로 두고 표시만 나누며, 섹션 목록에서 누락된 경로는 "기타" 섹션으로 떨어져 조용히 숨지 않는다. **D-059 완료**: `RailIconButton` 에 `hasFlyout` prop 추가, 하위 경로 2개 이상인 아이콘에 점 인디케이터를 붙여 즉시 이동 아이콘과 구분(②서비스 도메인 분산 문제는 PEP/APP 서비스 삭제로 이미 자연 해소). **추가 요청**: 메인 사이드바에서 클릭해야 열리던 flyout(그룹/즐겨찾기/Your Island) 을 마우스를 올리기만 해도 열리도록(hover-intent) 변경 — 레일을 스쳐 지나가는 오탐을 막는 150ms 오픈 지연, 아이콘→패널 이동 중 hover 가 끊겨도 안 닫히도록 200ms 닫기 지연(패널 쪽 `FlyoutShell.onMouseEnter` 가 취소), 여러 flyout 이 동시에 열리지 않도록 새로 열 때 나머지를 먼저 닫는 로직을 추가했다. 클릭은 기존처럼 즉시 토글되며 동작이 바뀌지 않는다. `NavFlyout.tsx` 의 `FlyoutShell` 에 `onMouseEnter`/`onMouseLeave` passthrough 를 추가해 사이드바가 hover 취소/재예약을 걸 수 있게 했다. lint/tsc/build 통과 |
| 2026-08-06 | 상단바 협업/문서 관리 hover 오픈 + 사이드바 중복 툴팁 제거 | 0 | 1 | claude-code | 사용자 제보("사이드바는 hover 로 잘 열리는데 상단바 협업/문서관리는 안 됨, 사이드바는 flyout 옆에 안 써도 되는 hover 박스가 같이 뜬다"). **상단바 hover 오픈**: `AppTopBar.tsx` 의 협업/문서 관리 드롭다운·즐겨찾기 버튼에 사이드바와 동일한 hover-intent(150ms 오픈/200ms 닫기)를 추가했으나, 처음엔 열리자마자 바로 닫히는 버그가 재현됐다 — 원인이 사이드바 때와 달랐다. 사이드바는 click-outside 캐처(`fixed inset-0`)가 레일(`<aside>`)과 **형제** 관계라 z-index 숫자 비교가 성립했지만, `AppTopBar` 의 캐처는 `<header>` **내부의 자식**으로 렌더돼 포지션 없는(`position: static`) 버튼들과 같은 스태킹 컨텍스트에서 경쟁 — 포지션 있는 캐처가 z-index 값과 무관하게 항상 위에 그려져 hover 중이던 버튼이 즉시 `mouseleave` 로 판정됐다. 버튼이 속한 행(날짜/그룹 버튼·즐겨찾기)에 `relative z-10` 을, 캐처엔 `z-0` 을 줘서 해결(같은 "hover 로 연 flyout 이 바로 닫힘" 증상이라도 스태킹 맥락이 다르면 처방도 다르다는 걸 확인). **사이드바 중복 툴팁 제거**: `RailIconButton` 이 `hasFlyout` 인 아이콘엔 hover 시 이름만 뜨는 툴팁을 더 이상 띄우지 않는다 — flyout 자체가 헤더에 라벨을 보여줘 툴팁과 겹쳐 보이던 문제. flyout 이 없는 아이콘(테마 토글 등)은 기존처럼 툴팁만 뜬다. Frontend: `components/layout/{AppTopBar,Sidebar}.tsx`. Playwright 로 상단바 hover 오픈(그룹 간 전환·패널 위 유지·클릭 토글)과 사이드바 툴팁 미노출을 수동 검증. lint/tsc/build 통과 |
| 2026-09-08 | R-4 6차 라운드 — 점검 체계 IA(매트릭스 중심 재편) 감사 (D-061~D-068) | 8 | 0 | claude-code | 사용자 3라운드 피드백("Deep Check/Addon 의미 확인" → "매트릭스가 첫 화면이어야" → "점검 방식 대신 실행 기술로 명명 + 등록·수정·테스트·운영적용·로그·히스토리") 기반. `PlatformStatusMatrix.tsx`·`CheckMatrixItemFormModal.tsx`·`CheckMatrixCellDetailModal.tsx`·`check_matrix_service.py`·`deep_checkers/*.py`·`checkers/*.py`·`batch_job.py`·`playbook.py`·`ops_check_service.py`·`homeStore.ts` 대조. 발견: 매트릭스가 홈 탭에 숨어있고 네비게이션에 없음(D-061), "Deep Check/Addon" 용어가 화면마다 다르게 노출되고 실행 기술이 뒤섞여 있음(D-062, etcd 4소스·4기술 중복 등), 드릴다운 링크 0건(D-063), 저장 전 테스트 API 는 있으나 매트릭스가 안 씀(D-064), cron 이 3곳 분산+클러스터 오버라이드 자동생성 없음(D-065), 배치잡/플레이북이 매트릭스·운영점검 콘솔에 실제 항목으로 안 옴(D-066), `Cluster.status` writer 2곳 덮어쓰기+심층점검 미반영(D-067), 애드온 등록 템플릿 type 불일치 버그(D-068). 설계안(그림 5종: 드릴다운 3단·명명 2축·라이프사이클 6단계·아키텍처·사용자 플로우)을 Artifact 로 발행. 코드 변경 없음 — 등재만 |
| 2026-09-09 | R-4 6차 라운드 로드맵 1단계 구현 (D-061 완료, D-062·D-063 부분) | 0 | 1.5 | claude-code | 사용자 승인("매트릭스 재편 로드맵 1단계"). **exec_tech 선언**: `DeepCheckTypeSpec.exec_tech` 필드 신설(기본 `k8s_api`) + 커스텀·하이브리드 등 11개 check_type 에 실제 기술 명시(kubectl/http/snapshot/promql), `list_check_types()` 직렬화에도 포함. addon 은 `checkers/__init__.py` 에 `EXEC_TECH` 매핑 신설(SDK 5종 `k8s_api`, HTTP 3종 `http`). `check_matrix_service._resolve_exec_tech()` 가 4개 source_type(core_bundle→`k8s_api`/manual→`manual`/deep_check→registry 조회/addon→`EXEC_TECH` 조회) 을 해석해 `_item_to_dict()` 출력에 `exec_tech` 추가(axios 인터셉터가 `execTech` 로 자동 변환, 스키마 변경 없음). **프론트**: `CheckMatrixItem.execTech` 타입 추가, 신규 `ExecTechBadge.tsx`(9개 코드 아이콘+한글 라벨, lucide-react Hexagon/Terminal/Globe/Activity/Camera/TerminalSquare/Code2/Workflow/Hand) 로 `PlatformStatusMatrix.tsx` 의 "핵심/Deep/Addon/수동" `SourceBadge` 를 완전히 교체. 매트릭스 헤더의 클러스터명을 클릭 가능한 링크로 바꿔 기존 `/ops-checks/:clusterId` 로 드릴다운(신규 `/clusters/:id` 는 3단계 예정). `homeStore.loadHomeTab()` 기본값 `work`→`platform` — 로그인 직후 플랫폼 현황 탭이 먼저 보임. 검증: 백엔드 import 스모크 테스트 + pytest 818건 collect(0 에러) + 관련 유닛 테스트 40건 통과(Postgres 미기동 샌드박스라 DB 의존 테스트는 CI 확인 필요), 프론트 lint/tsc/build 통과, docs-sync 통과. |
| 2026-09-09 | R-4 6차 라운드 로드맵 2단계 구현 (D-064·D-068 완료, D-065 부분) | 0 | 3 | claude-code | 사용자 요청("계속 후속 작업 진행"). **저장 전 테스트(B3)**: `check_matrix_service.list_catalog()`(신규, `GET /check-matrix/exec-techs`) — deep_check(REGISTRY)·addon(CHECKER_REGISTRY)·manual 을 실행기술별로 합친 카탈로그, 프론트 하드코딩 목록 제거. `preview_item()`(신규, `POST /check-matrix/items/preview`) — deep_check 는 `DeepCheckService.run_check_type_once(persist=False)`, addon 은 신규 `HealthChecker.preview_addon_check()`(세션에 추가하지 않는 transient `Addon` 인스턴스로 기존 `_dispatch()` 재사용) — 아무것도 저장하지 않고 1회 실행. 신규 커스텀 deep_check(`seed_default=False`) 등록 시 글로벌 정의가 없으면 `ensure_deep_check_definition()` 이 마법사 입력값으로 자동 생성(`ItemIn.thresholds/params` 신규 필드, `CheckMatrixItem` 컬럼 아님) — 이미 정의가 있으면(시드된 기본 타입 포함) 손대지 않는다. **copy-on-write(B4)**: `update_source_config()` — 해석된 deep_check 정의가 글로벌이면 저장 직전 클러스터 전용 사본을 만들어 그 위에 편집 적용(원본 글로벌 무변경), 응답에 `copied_from_global` 추가. `CheckMatrixRunbookPanel` 사전 경고("전 클러스터 적용")를 사후 안내("자동으로 클러스터 전용 사본 생성됨")로 교체. **등록 마법사(F2)**: `CheckMatrixItemFormModal` 을 신규 항목은 실행기술→종류→세부정보→값 설정→테스트→적용 6단계 마법사(`RegisterItemWizard`)로, 기존 항목 수정은 기존 단일 폼(`EditItemForm`)을 유지하되 하드코딩 `ADDON_TYPE_OPTIONS`/`useCheckTypes()` 를 카탈로그 API 로 교체 — 라디오 문구에서도 "Deep Check/Addon" 잔재 제거. **D-068**: 실제 애드온 등록 화면(`AddAddonModal.tsx`)이 처음부터 자체 `ADDON_TEMPLATES`(CHECKER_REGISTRY 키와 일치)를 썼고 `addons.config.ts`/`AddonConfig` 는 어디서도 import 되지 않는 죽은 코드였음을 확인 — 삭제로 처리. 검증: 백엔드 import 스모크 + `list_catalog()` 무DB 실행(28건: deep_check 19·addon 8·manual 1) + pytest 관련 유닛 28건 통과(Postgres 미기동이라 DB 의존 테스트는 CI 확인 필요), 프론트 lint/tsc/build 통과, docs-sync 통과. |
| 2026-09-09 | R-4 6차 라운드 로드맵 3단계 구현 (D-067·D-068 완료, D-063 부분 갱신) | 0 | 4 | claude-code | 사용자 요청("3단계 진행해줘"). **단일 롤업(B5)**: 신규 `services/cluster_status_service.py::recompute(db, cluster_id)` — ①최신 `DailyCheckLog` 가 pending(미연결)이면 다른 신호와 무관하게 전체 pending 으로 선제 종료 ②애드온은 opt-in 없이 항상 반영, 개별 pending 은 warning 으로 승격(예전 `HealthChecker.run_check()` 규칙 이관) ③심층 점검은 `DeepCheckDefinition.affects_cluster_status=True`(신규 컬럼, 기본 false)인 정의만 반영하고 그 결과가 pending 이면 "미판정"으로 집계 제외(애드온과 다른 규칙 — 노이즈 방지). `critical>warning>healthy` 우선순위로 집계해 `with_for_update()` 로 1회 기록하고 원인 목록(contributors)을 반환. `DailyChecker.run_daily_check()`/`HealthChecker.run_check()`/`HealthChecker.run_single_addon_check()`/`DeepCheckService.run_for_cluster()`·`run_definition_once(persist=True)` 다섯 곳 모두 `Cluster.status` 직접 대입을 지우고 자기 도메인 결과를 flush 한 뒤 이 함수만 호출하도록 배선(세션이 `autoflush=False` 라 flush 누락 시 recompute 가 방금 쓴 값을 못 봄에 유의) — `run_check_type_once`(정의 없는 ad-hoc 실행)는 opt-in 판정 대상이 없어 의도적으로 제외. `GET /clusters/{id}/status-breakdown`(신규, `routers/clusters.py`) 신설, `build_grid().clusters[]` 에 `status` 추가. 신규 `tests/test_cluster_status_service.py` 10건(pending 선제 종료, 애드온/심층 pending 처리 차이, 클러스터 전용/글로벌 정의 우선순위, 우선순위 등). **정의 폼**: `DeepCheckDefinitionForm` 에 "클러스터 종합 상태에 반영" 토글(`affects_cluster_status`, `DefinitionIn`/`Out` 스키마 추가) — 정의 복제/토글 재구성 호출부(`DeepCheckDefinitionList`/`DeepCheckSettings`) 도 필드 보존하도록 갱신. **/clusters/:id(F3)**: `OpsCheckConsolePage.tsx` 의 카탈로그·실행·로그 UI 를 `components/ops-check/ClusterOpsCheckPanel.tsx` 로 추출해 신규 `pages/ClusterDetailPage.tsx`(종합 상태+원인 카드 추가)와 공유 — `/ops-checks/:clusterId` 는 레거시 링크 호환을 위해 유지(내용은 같은 패널). `PlatformStatusMatrix` 의 클러스터명 링크를 `/ops-checks`→`/clusters` 로 전환, `ClusterCronBadge` 는 `coreHealth`(core_bundle 셀 상태만) 대신 롤업 `cluster.status` 사용. 대시보드 "Cluster Status" 카드에 롤업 상태+대표 원인 1줄+"클러스터 상세로" 링크. `navConfig.ts`/`Sidebar.tsx`(`CLUSTER_FLYOUT_SECTIONS`)/`panelRegistry.ts`(아일랜드 임베드)에 `/clusters` 등록. 검증: 백엔드 import 스모크 + pytest 828건 collect(0 에러, 신규 10건 포함) + 관련 유닛 40건 통과(Postgres 미기동이라 recompute() 자체 DB 테스트는 CI 확인 필요), 프론트 lint/tsc/build 통과, docs-sync 통과. |
| 2026-09-10 | R-4 7차 라운드 — Main UI 상용화 진단 (앱 셸·홈·로그인, D-069~D-082) + 2회차 정량 재집계 | 14 | 0 | claude-code | 사용자 요청("Main UI 상용화를 위한 UX/UI 개선 요청 진단"). **코드 변경 없음 — 등재만.** 셸 핵심부(NavFlyout·툴팁)의 `zinc-*` 고정 팔레트 57건이 1회차 grep 패턴(`gray-*`) 누락으로 미검출돼 있던 것을 발견해 지표 신설. 우선순위 P0(D-069/070/071/081/075) → P1(D-073/080/082/079/077) → P2(D-074/076/072/078). 로드맵 R-6(i18n)·R-7(세션/SSO) 신설 |
| 2026-09-10 | R-4 7차 라운드 P0 처리 (D-069/070/071/075/081) | 0 | 5 | claude-code | 사용자 요청("P0 5건 처리해줘"). 브랜드 통일+버전 표기, 테마 FOUC 제거(index.html 선적용 스크립트), 셸 flyout/툴팁 토큰화(layout 폴더 zinc/amber 0건), `usePageTitle` 라우트별 탭 제목, 레거시 Header 삭제. lint/tsc/build·docs-sync 게이트 통과 |
| 2026-09-10 | 사용자 제보 — 배치잡 즉시 실행 우회 버그 + 매트릭스/실행 방식 탭 UX 개선 (D-083~D-085) | 3 | 3 | claude-code | 사용자 제보("batchjob 수행을 껐는데 k8s cluster job 이 실행되는 것 같다" + "K8S BATCHJOB 켜고 끄는 기능을 명확하게" + "점검 항목 늘어나도 한눈에" + "실행 단계 재생 클릭시 색표현 안되는 버그, 스크롤 없이 한눈에"). AskUserQuestion 으로 두 가지 사용자 결정 확보: ①매트릭스 그룹핑은 "컴포넌트/영역별 그룹핑+접기"(위험만 자동 펼침) ②배치잡 "비활성화"의 의미는 "앞으로 새 실행만 막음"(이미 뜬 K8s 리소스는 안 건드림). 같은 날 별도로 진행된 R-4 7차 라운드(위 두 행, D-069~D-082)와 ID 대역이 겹쳐 이 세 항목은 D-083~D-085 로 재번호. **D-083(완료)**: `POST /batch-jobs/{id}/run` 에 `enabled` 가드 추가(스케줄 디스패처·일괄 실행은 이미 지키고 있었음), `BatchJobRow`/`BatchJobSlideOver.RunForm` 도 동일 가드+`PowerOff` 배지+사유별 툴팁. **D-084(완료)**: `PlatformStatusMatrix` 행을 category 기준 그룹(K8s/네트워크/스토리지/OS/앱)으로 묶어 접기/펼치기 + 그룹 헤더 정상·경고·위험 카운트 배지, 위험/경고 있는 그룹만 기본 자동 펼침(이후 사용자 선택은 `localStorage` 유지), 기존 페이지네이션(화면당 표시 행 수)은 이 그룹 접기로 대체. **D-085(완료)**: `CheckMatrixRunbookPanel` 의 `steps={running ? undefined : ...}` 억제 제거(백엔드가 이미 실행 중 단계를 `running` 으로 채워 2초 폴링 중이었음) + 대상/최근수행 1행·명령/설정 2열 레이아웃 재배치(탭 전용 모달 폭 `max-w-4xl`, 목록별 `max-h-72` 내부 스크롤 캡). 검증: 신규 `tests/test_batch_job_run_endpoint_guard.py`(DB-free) 통과, pytest 829건 collect(0 에러) + 관련 배치잡 유닛 27건 통과(Postgres 미기동이라 DB 의존 10건은 CI 확인 필요), 프론트 lint/tsc/build 통과, docs-sync 통과. |
